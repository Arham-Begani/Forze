# Forze — Progress Log

## How to Use This File
Update this at the end of every session.
Read this at the start of every session before opening Gemini Code.
This file is the Agent's memory between sessions.

---

## Current Status
**Phase:** 25 — Perf (passes 1+2) + robustness: load delay, auth refresh-token loop, Gmail OAuth (COOP), bootstrap consolidation, error boundary + no-regression rules
**Last updated:** July 9, 2026

### Latest Session (July 9 2026) — Performance pass 1: make feature loads feel instant (safe/reversible wins)
- User: "make this platform way more faster and way more robust… it currently takes too long to even load a feature… make it faster by any means so everything happens in milliseconds," while insisting **previous features must not regress**. Diagnosed the actual load path end-to-end before touching anything, then shipped only build-verified, behavior-preserving changes.
- **Root causes found (measured, not guessed):** opening one module (e.g. Landing Page) = (1) a **hard ~1.25s artificial loading screen** — `DashboardLayout` renders `<LoadingScreen minimumDuration={800}>` and holds the whole app at `opacity:0` on a pure timer (800ms + 150 + 300 + a 400ms exit anim), NOT waiting on data; (2) **~5 separate client HTTP requests** (`/api/auth/session`, `/api/projects`, `/api/ventures`, `/api/billing/me`, `/api/ventures/[id]?module=`), **each** calling `supabase.auth.getUser()` (network round-trip to Supabase Auth) **and a `users` upsert WRITE**, via `getSession()`; (3) the **proxy/middleware also runs `getUser()` on every matched request** including those APIs (double auth); (4) leftover `console.log('[DEBUG blink effect]')` firing on every venture switch; (5) all of it is client-side after a 3,714-line `[module]/page.tsx` bundle downloads + hydrates.
- **Fixes shipped this pass (all surgical, build-verified — `tsc` + `npm run build` both exit 0):**
  1. **`app/dashboard/layout.tsx`** — `LoadingScreen minimumDuration` 800 → **200** (cuts ~1s of dead time off every dashboard load; sidebar already has skeletons so it degrades gracefully). Removed the two `console.log('[DEBUG blink effect]')` lines. Sidebar "blink" on venture switch left intact (it's a deliberate UX feature from phase 8857c00).
  2. **`lib/auth.ts`** — wrapped `getSession()` in React `cache()` so a single server render (layout + page + nested server components) shares ONE auth call instead of each re-hitting Supabase Auth. Added a module-scope `ensuredUserIds` Set so the FK-safety `users` upsert fires **at most once per user per warm instance** instead of on every request — same correctness (idempotent, re-runs on cold start) minus a blocking DB write on the hot path.
  3. **`next.config.ts`** — `compiler.removeConsole` in production (keep `error`/`warn`) to strip console noise from bundle+runtime; `experimental.optimizePackageImports` for `framer-motion`/`lucide-react`/`recharts`/`react-markdown` to tree-shake the heavy client bundles.
- **Deliberately NOT touched (risk > confidence without live runtime testing):** the proxy `getUser()` (canonical Supabase session-refresh — changing it can break auth), consolidating the 5 requests into one `/api/bootstrap` payload, and code-splitting the 3,714-line module page. These are the next tier — see Next.
- **Honest expectation set with user:** feature *loading/navigation* can be made near-instant; but AI *agent runs* (landing page gen, shadow board) call Gemini and inherently take seconds→tens of seconds — that's model latency, not app latency, and can't be made "milliseconds."
- **Next (pass 2, needs live smoke-test against real Supabase before shipping):** (a) narrow the proxy `matcher` so public/API routes don't pay for a middleware `getUser`; (b) add a single `/api/bootstrap` endpoint returning `{session, projects, ventures}` and collapse the layout's 3 fetches into 1 (≈3× fewer auth round-trips per load); (c) `React.lazy`/dynamic-import the heavy branches of `[module]/page.tsx` (recharts, PDF, preview iframe) so first paint ships less JS; (d) add `loading.tsx` for `venture/[id]` (server-fetched) for instant skeletons; (e) confirm DB indexes on `venture_members(user_id)`, `ventures(project_id)`, `conversations(venture_id, module_id)`.

### Same day (July 9 2026, part 2) — Gmail OAuth fixes + auth refresh-token loop + robustness hardening
- User hit three live errors and asked to make the platform robust so "changing one feature doesn't break another," and to codify that in CLAUDE.md. All fixes build-verified (`tsc` + `npm run build` exit 0).
- **Gmail `Error 400: invalid_request` (redirect_uri):** root cause is Google Cloud Console not having the exact redirect URI whitelisted; the suspicious space in the reported URI (`forze.in /api/...`) points to a trailing space in `NEXT_PUBLIC_APP_URL`. Told user to (1) add `https://www.forze.in/api/integrations/gmail/callback` (+ apex + `https://localhost:3000/...`) to the OAuth client's Authorized redirect URIs, (2) strip whitespace from `NEXT_PUBLIC_APP_URL` in Vercel and **redeploy** (it's a build-time `NEXT_PUBLIC_` var), (3) keep www-vs-apex canonical consistent. **Code hardening:** `lib/gmail-oauth.ts` `getAppUrl()` and `app/api/integrations/gmail/callback/route.ts` now `.trim().replace(/\/+$/,'')` the app URL so stray whitespace/trailing slash can't corrupt the redirect.
- **`AuthApiError: refresh_token_not_found` (×4):** this is the Supabase *login* session, not Gmail. The 4-at-once = the dashboard's ~5 parallel per-load requests racing on refresh-token rotation (first rotates it, the rest fail). Found + fixed a **real bug in `proxy.ts`**: the `!user` → `/signin` and `user` → `/dashboard` redirects returned a fresh `NextResponse.redirect` that **dropped `supabaseResponse`'s Set-Cookie headers** — so the stale/invalid auth cookie was never cleared and every subsequent request re-failed the refresh in a loop. Added a `redirectTo()` helper that copies `supabaseResponse.cookies` onto the redirect (canonical Supabase-SSR pattern) so an invalid session gets cleanly cleared. Immediate user remediation: sign out / clear cookies once to mint a fresh token.
- **Gmail `Cross-Origin-Opener-Policy would block the window.closed call`:** the Gmail connect used a **popup + `popup.closed` polling** in THREE places (`components/marketing/ConnectedChannelsPanel.tsx`, `components/venture/CreateCampaignFlow.tsx`, `components/venture/DirectMailFlow.tsx`). Google's OAuth pages send `COOP: same-origin`, severing the popup handle → `popup.closed` is blocked → "Connecting…" hangs forever. Switched all three to the **full-page redirect** the social providers already use (immune to COOP). Threaded a sanitized same-origin `returnTo` through the signed OAuth state so users land back on the exact page: `signOAuthState(userId, returnTo)` + new `getReturnToFromState()` + `sanitizeReturnTo()` guard (single leading `/`, blocks `//` and `/\` open-redirects) in `lib/gmail-oauth.ts`; POST `/api/integrations/gmail` passes `body.returnTo`; the callback redirects to `${appUrl}${returnTo}?gmail_connected=1&email=…`. `ConnectedChannelsPanel` gained an effect that consumes `?gmail_connected/?gmail_error`, refreshes status, and strips the params.
- **Robustness (structural):** new **`app/dashboard/error.tsx`** route error boundary — a crash in any module/page now degrades to a recoverable "Try again / Back to dashboard" card while the layout + sidebar stay mounted, instead of white-screening the app (there were previously **zero** error boundaries in the repo). Added a **"Robustness & No-Regression (Non-Negotiable)"** section to `.claude/CLAUDE.md`: isolate failures / guard every external result, additive-over-destructive schema+API changes, high-blast-radius file list (auth/supabase/queries/billing/proxy/layout), and a mandatory verify step (`tsc` + `build` + smoke-test one unrelated feature).
- **Next:** live-verify Gmail connect end-to-end once the Google Console redirect URIs are added; consider finer-grained error boundaries around the heaviest panels (CRM, campaigns); apply the same `returnTo`/redirect pattern audit to LinkedIn/Instagram (`lib/marketing-oauth.ts`) if they ever move to popups; then perf pass 2 (bootstrap endpoint + code-splitting).
- **Committed:** 12 granular commits to `arhambegani2@gmail.com`, no co-authors (6f37274 → 98cb026). The pre-existing whitespace-only reindent of `db/migrations/043_*.sql` was deliberately left uncommitted (not this session's change).

### Same day (July 9 2026, part 3) — Perf pass 2: consolidate dashboard bootstrap + instant venture skeleton
- Build-verified (`tsc` + `npm run build` exit 0; `/api/bootstrap` in the route table).
- **New `app/api/bootstrap/route.ts`:** returns `{ session, projects, ventures }` in ONE request — `requireAuth()` once, then `Promise.all([getBillingSnapshot, getProjectsByUser, getVenturesByUser])`. Response shapes mirror `/api/auth/session` + `/api/projects` + `/api/ventures` EXACTLY (same session fields, same `completedModules` computation via a copied `COMPLETED_MODULE_MAP`) so the client consumes it identically.
- **`app/dashboard/layout.tsx`:** `load()` now calls `/api/bootstrap` first and only falls back to the original three parallel fetches (`loadIndividually()`) if it fails — so a bootstrap failure can never blank the dashboard. This cuts the layout's per-load auth work from **3 requests → 1** (3× fewer Supabase Auth round-trips + proxy `getUser`s) AND removes the concurrent refresh-token race that was a source of the `refresh_token_not_found` bursts fixed in part 2.
- **New `app/dashboard/venture/[id]/loading.tsx`:** first `loading.tsx` in the repo — an on-brand `.skeleton` header + 6-card grid streams in instantly while the server component runs `getVenture()`, so navigating to a venture no longer shows a blank main area during the DB round-trip.
- **Deferred (still needs live runtime testing / DB access before shipping — kept out per the new no-regression rules):** code-splitting the 3,714-line `[module]/page.tsx` (dynamic-import recharts/PDF/preview); narrowing the proxy `matcher` (risky — could drop session refresh on API routes); a `CREATE INDEX IF NOT EXISTS` migration for `venture_members(user_id)` / `ventures(project_id)` / `conversations(venture_id, module_id)` (I can't apply migrations to the live DB; would write the file for the user to apply).
- **Next:** live smoke-test the dashboard loads via `/api/bootstrap` (confirm session/projects/ventures all populate and the fallback still works if it 500s); then tackle code-splitting the module page behind a careful runtime test.

### Same day (July 9 2026, part 4) — Venture creation: 10–15 min → sub-second (AI naming off the critical path)
- User: "after I enter my idea and click make venture it takes wayyy too long — up to 10–15 min." Traced `app/dashboard/new/page.tsx` `handleSubmit`: it ran **5 sequential awaited steps**, and **step 1 was a blocking Gemini call** to `/api/generate-name` (`getFlashModel()` → `gemini-3-flash-preview`, `maxOutputTokens 32768`, **no timeout**). A venture insert is milliseconds; an unbounded model call is the only multi-minute-capable op in the chain (slow/hung/rate-limited model, cold start, or a thinking model burning the 32k output budget). Greeting page's create path does NOT call the namer, so only `/new` was affected.
- **Fix (client-only, `app/dashboard/new/page.tsx`):** name the project **instantly and locally** via new `deriveNameFromIdea()` (stopword-filtered, title-cased, ≤3 words), create the project with it, then run the idea-save PATCH and the venture POST **in parallel** (both only need `project.id`), then navigate immediately. AI naming moved to a **fire-and-forget** `refineProjectNameInBackground()` (20s AbortController timeout; PATCHes the project name + dispatches `Forze:refresh-projects` only if the AI name differs) and the first-idea save to `saveFirstIdeaInBackground()`. Net: create flow is now ~2–3 fast DB round-trips (project → parallel[patch, venture]) with zero AI on the path; the nicer AI name swaps in seconds later, invisibly.
- No API/schema changes; `/api/generate-name` untouched (just no longer blocks the user). Idea-guard-rail safe: `global_idea` is still saved before navigation.
- Verified `tsc` + `npm run build` exit 0.
- **Next:** consider a small server-side timeout on the Gemini call in `/api/generate-name` (and other one-shot Gemini utility routes) so background/other callers can't hang a serverless invocation for minutes; audit other "click → AI → wait" critical paths for the same optimistic pattern.

### Previous Session (July 6 2026) — Fix "Not found" agent-run failures across ALL modules (owner lockout)
- User hit "Agent run failed — Not found" (single stream line "Not found") running the Landing Page (Pipeline) module, and asked to make sure it never happens in any module again.
- **Root cause (traced end-to-end, not the model):** the Gemini Flash model (`gemini-3-flash-preview`) was verified live-working (REST + the exact SDK `startChat/sendMessageStream` call both return 200/OK), so this was NOT a discontinued-model 404. The "Not found" is the frontend rendering a **404 from `POST /api/ventures/[id]/run`**: `getVenture()` → `getVentureAccess()` returned `null`, so the route returned `{error:'Not found'}` *before* creating a conversation (which is why no failed landing conversation exists in the DB). The frontend `executeRun` catch sets `lines:[error.message]` → the entry's only stream line is "Not found" and `getEntryErrorMessage` shows "Not found".
- **Why access resolved to null:** migration 024 backfilled `venture_members` only from ventures that existed when it ran; **`createVenture` never inserted an owner membership row**; and `getVentureAccess` only fell back to `ventures.user_id` ownership when the table was *missing* (`isMissingRelationError`). So every venture created *after* 024 had no membership row and its true owner was locked out of **every** module run. Verified against the live DB: **4 ventures** (MotoLedger, Blue Willow Sync, Bharat AI Shiksha, ModelTrace) have an owner with no `venture_members` row; 0 duplicates.
- **Fixes (all in `lib/queries.ts`):**
  1. **`getVentureAccess`** — `.single()` → `.maybeSingle()` (zero rows is no longer a PGRST116 error), and the owner fallback to `ventures.user_id === userId → 'owner'` now runs **whenever no membership role resolved** (not only when the table is missing). This is the single chokepoint `getVenture()` gates on, so it un-breaks every module at once and can never lock a real owner out again. Still owner-scoped (only grants 'owner' on a user_id match).
  2. **`createVenture`** — now seeds the owner `venture_members` row via new `ensureVentureOwnerMembership()` (admin/service-role client, since `venture_members` RLS has no INSERT policy; idempotent upsert; non-fatal — the fallback covers any write failure). Closes the gap going forward so member-scoped listings are also correct.
- **New migration `db/migrations/043_backfill_missing_venture_members.sql`** — idempotent `INSERT ... SELECT ... WHERE NOT EXISTS ... ON CONFLICT DO NOTHING` to seed owner rows for any venture missing one. **NOT applied to the live DB** (repo has no migration runner; not auto-writing to prod). Access for the 4 broken owners is already restored at runtime by fix #1 once deployed; applying 043 additionally makes them appear in member-scoped listings (`getVenturesByUser`).
- Verification: `npx tsc --noEmit` exit 0; `npm run build` exit 0. Live-probed the Gemini model + the exact SDK call (both succeed) and the live DB (confirmed the 4 owner-lockout rows) to nail the root cause before touching code.
- **Next:** deploy so the 4 owners (and all future ventures) can run modules; optionally apply migration 043 for listing/data hygiene; consider making the run/stream 404 message less generic than "Not found".

### Previous Session (July 4 2026, part 3) — Outreach Phase 3: AI Lead Scout + post-pivot content brief
- Continuation of the outreach overhaul plan. Two deliverables: Forze now FINDS leads (the core "no real use" complaint), and every content generator stops running on the null context keys left behind by the pivot.
- **New `lib/outreach-brief.ts`:** `buildOutreachBrief(ventureName, context, {globalIdea})` — composes venture positioning from what EXISTS post-pivot: landing copy (hero headline/subheadline, features, SEO one-liner), shadow board (verdict + top weaknesses as "objections to pre-empt"), founder's own idea text, plus legacy research/branding/marketing when an older venture still has them. Wired into: `buildVentureBrief` in `lib/routine-executor.ts` (gmail routine emails), IG + LinkedIn routine draft calls, `generateFreshInstagramDrafts`/`generateFreshLinkedInDrafts` (new optional `brief` param — brief becomes primary context, legacy enriches), the marketing assets route ("Create X Drafts"), and `generate-email` (campaign email gen now enriches the client-sent description with the venture's real positioning server-side). The stale **"Run the marketing module first"** error (a module that no longer exists) is gone — replaced with an honest 502 + "publish a landing page first for better context" hint.
- **New `agents/lead-scout.ts` — the AI Lead Finder:** two-step. (1) `generateIcpDraft` (Flash): turns the outreach brief into an editable Ideal Customer Profile. (2) `runLeadScout` (`getProModelWithSearchAndThinking`, live web search, 4-min timeout + retry): finds real prospects — people/companies/communities — as Zod-validated candidates `{kind, name, company, role, sourceUrl, email|null, whyRelevant, confidence}`. **Email guardrail is contractual:** only emails literally visible in a read source; pattern-guessing explicitly forbidden in the system prompt; null email is the expected common case; everything surfaces as "unverified" in the UI and `campaign_leads.verified` stays false.
- **New `POST /api/campaigns/[id]/scout-leads`:** `{mode:'icp'}` (cheap, rate-limited) → ICP draft; `{mode:'scout', icp}` → burns one weekly `lead_scout` action then searches. `maxDuration 300`.
- **Billing (migration 042 + `lib/billing.ts` + `lib/billing-queries.ts`):** new `lead_scout` ActionId under the `outreach` feature gate — Builder 2 / Pro 10 / Studio 30 runs per week (free/starter 0); `feature_usage_counters` CHECK widened; all usage/limit/humanize plumbing extended. **Apply migration 042 with 033–041.**
- **Wizard:** "AI Lead Finder" is now the first lead source in `CreateCampaignFlow` (the dead Twitter stub is gone) — auto-drafted editable ICP → "Find prospects" (shows weekly-run cost) → candidate checklist: email-bearing candidates preselected with amber "unverified email" chips; email-less ones shown as research targets ("no public email") with source links, not selectable. Selected candidates flow into the normal lead-upload path (person → first name; company/community → org name as company).
- Verification: `npx tsc --noEmit` exit 0; `npm run build` exit 0 (`/api/campaigns/[id]/scout-leads` in route table). Live scout quality NOT yet tested against real Gemini web search — see Next.
- **Next:** apply migrations 033–042; live-run a scout on a real venture (validate candidate/source quality + the email guardrail holds); then Phase 4 (approve-then-auto sequences, reply-draft suggestions, performance feedback loop, weekly digest).

### Previous Session (July 4 2026, part 2) — Outreach Phase 2: landing-page leads flow into campaigns
- Continuation of the outreach overhaul (plan: `~/.claude/plans/make-me-a-plan-swift-kahn.md`). Built ON TOP of the parallel CRM-roadmap session below — kept off its files except two shared integration points (`app/api/ventures/[id]/leads/route.ts`, and committing its self-contained `lib/schemas/crm.ts` as a build dependency of that route). The CRM session's read-only outreach merge stays; "retire System A" from the original Phase-2 plan is dropped in favor of that approach.
- **Migration 041 (`041_lead_campaign_bridge.sql`):** `campaign_leads.lead_id → leads.id` back-link (nullable, SET NULL on delete) + `campaigns.auto_enroll_landing_leads` + partial indexes. **Apply after 034–040.** (Numbered 041 because the parallel session took 034–040.)
- **New `lib/lead-capture.ts`:** `captureLandingLead()` — public landing-page capture now uses the ADMIN client. This also fixes a latent break: migration 034 enables venture-members-only RLS on `leads`, which would have blocked every anonymous landing-page form submit (the capture endpoint has no session). Same pattern as the HMAC tracking routes. `autoEnrollLeadInCampaigns()` — new leads are upserted as pending recipients into every venture campaign with `auto_enroll_landing_leads=true` and status scheduled/active; the outreach cron then emails them on the campaign's drip schedule (the full "page → pipeline" loop, zero manual steps). `advanceCrmLeadStatus()` — forward-only status transitions, never demotes won/lost.
- **New `app/api/campaigns/[id]/leads/from-crm` (GET+POST):** GET lists the venture's email-reachable CRM leads with an `alreadyEnrolled` flag; POST enrolls selected lead ids into the campaign (venture-scoped, upsert-deduped, lead_id back-linked).
- **Status auto-advance:** campaign send (cron `sendPendingBatch` + interactive send route) moves a linked CRM lead `new → contacted`; a reply (cron reply-sync + manual poll-replies) moves `new/contacted → qualified`. CRM pipeline now reflects outreach reality instead of being 100% manual.
- **Wizard (`CreateCampaignFlow`):** Step 2 gained "Your captured leads" as a first-class source (replaces the dead YouTube stub) — checkbox picker with already-enrolled rows disabled, select-all default; send path enrolls via from-crm instead of CSV upload. Delivery panel gained the "Auto-enroll new landing-page leads" toggle (persisted via `SendCampaignSchema.autoEnroll` → `auto_enroll_landing_leads`).
- Verification: `npx tsc --noEmit` exit 0; `npm run build` exit 0 (`/api/campaigns/[id]/leads/from-crm` in route table). `lib/lead-capture.ts` compiles against both HEAD's and the WIP's `Lead` interface.
- **Next:** apply migrations 034–041 together; smoke-test capture→auto-enroll→cron-send→status-advance end-to-end; then outreach Phase 3 (AI Lead Finder + `buildOutreachBrief`) and Phase 4 (approve-then-auto + feedback loop).

### Previous Session (July 4 2026) — CRM Feature Roadmap (Phases 0–6, per saved plan `~/.claude/plans/make-a-plan-to-zazzy-pixel.md`)
- Context: user asked for a plan to make the CRM module "better and more useful." Three-agent research pass (frontend/backend/product) + a Plan-agent design found a functional-but-shallow CRM (real DB leads + one-shot dispatch + basic analytics, but a fully-built-and-never-called Gmail replies endpoint, no RLS on any CRM table, no lead detail/notes, a "Pipeline" tab that was actually just campaign stats, and social leads that were virtual/non-persisted). User chose: fix RLS first, build a **real** deals/stages pipeline (not a cheap status-as-kanban shortcut), do a **lightweight read-only merge** of the two disjoint outreach systems (not full convergence), and **materialize social leads into `leads`**. Implemented all 6 phases in one session.
- **Phase 0 (security):** `db/migrations/034_crm_rls.sql` enables RLS on `leads`/`analytics_events`/`outreach_campaigns`/`outreach_messages`/`testimonials`/`platform_feedback` — none had ever had it, and the app's Supabase client is anon-key/session-scoped, so this was a real cross-tenant read/write gap via PostgREST, not theoretical. Policies join through `venture_members` (covers invited collaborators, not just the owner) rather than mirroring the older `015_campaign_rls.sql`'s owner-only pattern. **NOT YET APPLIED to the live DB — see Next.** New `lib/schemas/crm.ts` consolidates the 3 inline CRM Zod schemas + a new one; `app/api/ventures/[id]/leads/route.ts` (public landing-page capture) now Zod-validates instead of hand-rolled regex, same response shape/behavior.
- **Phase 1 (quick wins):** Orphaned `GET crm/replies` (fully built, never called) now powers a real "Replies" panel with a manual "Check for replies" button. Dead LinkedIn/Gmail/Reddit/Telegram inbox filters removed (aggregator only ever produced Instagram). `window.confirm()` replaced with the shared `ConfirmModal` everywhere.
- **Phase 2 (lead depth):** `035_crm_leads_unify_social.sql` makes `leads.email` nullable + adds `external_identity` so Instagram commenters become real, upserted `leads` rows (`crm/leads/route.ts` now syncs-then-returns instead of computing virtual rows) — one lead model, not two. `036_crm_lead_fields.sql` adds `company/phone/tags/owner_id`. `037_crm_lead_activity.sql` adds `lead_activity` (notes + auto-logged status/field/email/deal-stage changes, RLS included from day one this time). New `LeadDetailDrawer.tsx` (edit fields, notes timeline, delete, convert-to-deal) + bulk status/delete on the Leads table (`crm/leads/bulk/route.ts`).
- **Phase 3 (real pipeline):** `038_crm_deals_and_stages.sql` — `pipeline_stages` (per-venture, customizable, lazily seeded: New/Contacted/Qualified/Proposal/Won/Lost) + `deals` (linked to a lead, value/probability/close date/lost reason). Deals are **only** ever created via an explicit "Convert to Deal" action — no backfill from existing leads, so the pipeline never has fabricated historical data. New `PipelineTab.tsx`: real drag-and-drop kanban via newly-added **`@dnd-kit/core`/`sortable`/`utilities`** (no DnD library existed in the repo before), dragging a card PATCHes `deals/[dealId]` and logs a `deal_stage_change` activity row.
- **Phase 4 (outreach visibility):** new `crm/outreach-activity/route.ts` merges CRM-native `outreach_campaigns` and the separate Campaigns/Auto-GTM system's `campaigns` into one clearly-labeled ("CRM" vs "Campaigns" badges) read-only list — no data-model convergence, explicitly deferred as its own future project.
- **Phase 5 (reply persistence + AI):** `039_crm_outreach_replies.sql` adds `outreach_messages.subject/body` (needed so `analyzeReply()` has the original message to compare against) + new `outreach_replies` table. `lib/gmail-replies.ts`'s `syncCrmReplies()` (replaces the old ephemeral `fetchCrmGmailReplies`) persists + classifies new replies via the existing `analyzeReply()` Gemini primitive (reused from the Campaigns system, not reinvented) — wired into both the manual "Check for replies" button (immediate, session-scoped) and a new admin-client background sweep (`lib/crm-replies-cron.ts` + `app/api/cron/poll-crm-replies/route.ts`, registered in `vercel.json`, mirrors `run-outreach`'s auth pattern).
- **Phase 6 (polish):** `CrmDashboardClient.tsx` (was 1523 lines, one file) decomposed into `components/venture/crm/{shared,OverviewTab,InboxTab,RepliesPanel,LeadsTab,LeadDetailDrawer,PipelineTab,OutreachTab}.tsx`. Framer Motion added (tab-switch transitions + `layoutId` active-tab underline, drawer slide-in) — previously zero motion existed in CRM despite the rest of the dashboard using it heavily.
- Verification: `npx tsc --noEmit` exit 0; `npm run build` exit 0 (all new routes in the table: deals, deals/[dealId], pipeline-stages(+[stageId]), outreach-activity, leads/bulk, leads/[leadId]/activity, cron/poll-crm-replies). Browser-smoke-tested against a real venture (Overview/Inbox/Leads/Outreach tabs all render and behave correctly, including the Replies "Check for replies" button's graceful empty-state path). **Pipeline and Deals tabs currently 500 in the browser** — expected and confirmed via server logs ("Could not find the table 'public.pipeline_stages'/'public.deals'"): migrations 034–039 were deliberately NOT applied to the live Supabase project (no migration-runner exists in this repo; applying RLS/schema changes to production without confirmation is exactly the kind of hard-to-reverse action this agent holds back on). Every other tab degrades gracefully (loading/error states, no crashes) rather than breaking the page.
- **Next (blocking for full functionality):** apply `db/migrations/034_crm_rls.sql` through `039_crm_outreach_replies.sql` in order via the Supabase SQL Editor (or `psql`) against the live project — test on a branch/staging copy first per the plan's own risk note (RLS on tables with existing rows + an unverified ownership-column assumption can silently reject legitimate writes). Once applied: Pipeline/Deals/lead-detail-drawer/bulk-actions/persisted-replies all become live. After that: live-smoke-test the kanban drag end-to-end, dispatch a real campaign + confirm the reply-sync cron classifies a reply, and consider Phase 5.4 (lead scoring, deliberately deferred — no existing primitive to reuse, unlike reply classification). CLAUDE.md/PRD.md are still stale re: the post-pivot module set (flagged during research, out of scope for this roadmap).

### Previous Session (July 3 2026, part 2) — Outreach Overhaul Phase 1: sequences, schedules, and auto-sync become real
- Context: user asked for a plan to make Outreach genuinely useful ("I don't see any real use of it"). Deep audit found the feature was a set of half-built loops: follow-ups existed in DB/UI but nothing sent them; schedule/stagger options were accepted then ignored (always an immediate blast); scheduled social posts NEVER published (dispatch cron auth rejected Vercel Cron); reply sync was a manual button; open/click counters over-counted; bounce handling was dead scaffolding. Full 4-phase plan approved + saved (`~/.claude/plans/make-me-a-plan-swift-kahn.md`); this session shipped **Phase 1**.
- **Fix (1a): `app/api/marketing/publish/dispatch/route.ts`** now accepts GET+POST with the same auth set as run-routines (`x-vercel-cron` / `Bearer CRON_SECRET` / legacy custom header). Scheduled Instagram/LinkedIn posts actually publish now — previously every cron invocation 401'd forever.
- **Migration 033 (`db/migrations/033_outreach_engine.sql`):** `campaign_status` enum + `'scheduled'` (guarded ALTER TYPE); `campaign_leads` + `gmail_message_id`/`gmail_thread_id` (thread linkage captured at send); `campaigns` + `daily_send_cap`, `last_replies_polled_at`; `gmail_integrations.daily_send_limit` default 2000→500 (+ data migration) — consumer Gmail's real limit is ~500/day. **Must be applied to the DB before scheduled/drip/follow-up sends work.**
- **New `lib/outreach-executor.ts` + `app/api/cron/run-outreach/route.ts`** (vercel.json cron, every 10 min; auth mirrors run-routines). Each tick: (1) promotes due `scheduled` campaigns to active; (2) sends pending leads for active campaigns — drip mode respects `daily_send_cap` (default 50/day, remaining allowance computed from `campaign_analytics` today-row), all_now continues in 100/tick batches; per-user Gmail health cached per tick and allowance decremented so multiple campaigns can't blow the daily limit; (3) **follow-ups become real**: eligible = sent + no reply + not suppressed + `followup_count < max_followups` + last touch older than `followup_delay_hours`; CAS-claim on `followup_count` prevents double-sends from overlapping crons (claim released on send failure); body = user's `followup_message` or Gemini-generated per (campaign, touch#) with angle progression (bump → new value point → graceful close), personalized per lead, sent **threaded** into the original Gmail conversation (`sendEmailViaGmail` gained `threadId`; subject `Re: <original>`); (4) **auto reply-sync** with fair rotation via `last_replies_polled_at` (30-min min interval, 10 campaigns/tick): thread-id matching beats sender-address matching (catches alias replies), Gemini reply analysis stored, lead marked replied — which is also what pauses its sequence; (5) **bounce sync**: new `pollGmailForBounces` in `lib/gmail-sender.ts` scans mailer-daemon notices (one scan per user per tick, cached), extracts failed recipients, suppresses leads (`bounced_at` guard makes reprocessing a no-op), increments bounced metrics; (6) campaigns with nothing left to send and no remaining follow-up work auto-complete.
- **Send route (`app/api/campaigns/[id]/send/route.ts`):** future `scheduledTime` or `sendMode:'staggered'` now **defers to the cron** — persists approved subject/body + sequence settings on the campaign row, sets status `scheduled`, returns 202. Immediate sends also persist copy+settings (so follow-ups work after a manual blast) and store gmail message/thread ids via extended `markLeadSent`. Billing gate moved after the eligible-leads + Gmail-connected checks — a no-op send no longer burns a weekly `campaign_send` unit.
- **Honest analytics:** `app/api/track/pixel|click` now increment campaign counters + daily analytics ONLY on the first open/click per lead (previously every reload/Gmail-proxy prefetch inflated the totals; the guarded lead update's `.select('id')` result gates the RPCs).
- **UI:** `CreateCampaignFlow` — RFC-4180-ish CSV parsing (quoted commas survive), real `.csv` file upload, and a Delivery panel in Review: Send now vs Schedule (datetime-local), All-at-once vs Drip N/day, follow-up toggle + wait-days + max touches (defaults ON, 3d, 2). Deferred sends show "Schedule Campaign" and handle the 202. `CampaignDetail` — renders the previously-computed-but-never-shown 30-day engagement timeline (CSS bar chart: sent/opened/replied), shows drip cap + follow-up cadence + scheduled start in Config, `scheduled` status chip; replies empty-state notes auto-sync. `CampaignList` gained the `scheduled` chip style.
- **Zod:** `SendCampaignSchema` gained `dailyCap`/`scheduledTime` (validated ISO)/`enableFollowups`/`followupDelayHours`/`maxFollowups`; campaign/lead schemas track the new columns; `daily_send_limit` default 500.
- Verification: `npx tsc --noEmit` exit 0; `npm run build` exit 0 (`/api/cron/run-outreach` in route table). NOT yet live-tested against a real Gmail account — see Next.
- **Env note:** optional `OUTREACH_CRON_SECRET` (falls back to `ROUTINES_CRON_SECRET`/`CRON_SECRET`/`x-vercel-cron`). No new required env.
- **Next:** apply migration 033; live smoke-test (create campaign with 2 test leads + follow-ups at short delay → verify cron sends touch 2 threaded, reply pauses, bounce suppresses; schedule a social post → verify auto-publish). Then Phase 2 (one lead pool: landing-page leads → campaigns, CRM 1:1 sends, funnel view), Phase 3 (AI Lead Finder + `buildOutreachBrief` to fix post-pivot content degradation), Phase 4 (approve-then-auto + feedback loop) per the saved plan.

### Previous Session (July 3 2026) — THE PIVOT: Build Section Reduced to Landing Page + Shadow Board + Co-pilot
- User: "forze will be making a huge pivot, remove everything in the build section except landing_page, shadow board and co-pilot... the removed parts can be done by any ai." Rationale: research/branding/marketing/feasibility reports are one prompt away in any chatbot — Forze now focuses on what generic AI can't do (live deployed pages, adversarial board reviews, venture-aware co-pilot, outreach/CRM).
- **Modules REMOVED** (UI + run capability + plan access): `research` (Genesis), `branding` (Identity), `marketing` (Content Factory), `feasibility`, `full-launch` (Orchestrator). **Cohorts removed entirely** (they were full-launch multipliers): `app/api/cohorts/**`, `app/dashboard/cohort/**`, sidebar Cohorts section, `agents/cohort-comparator.ts`, `agents/variant-generator.ts`.
- **Modules KEPT:** `landing` (Pipeline), `shadow-board`, `general` (Co-pilot) in BUILD; Outreach (campaigns/routines/direct mail), CRM, Inspiration, Testimonials, Investor Kit untouched. `launch-autopilot` + `mvp-scalpel` backends kept (no UI trigger, unchanged). `ConnectedChannelsPanel` survives on the campaigns page (was also on the deleted marketing page), so social connect/publish + routines keep working. Campaign draft gen (`marketing-drafts.ts`) already null-guards a missing marketing brief. Kept agents (pipeline/shadow/general) already guard missing research/branding context — they run standalone.
- **Core registries:** `BillingModuleId` shrunk to 6 ids (landing/general/shadow-board/investor-kit/launch-autopilot/mvp-scalpel); labels + credit costs trimmed; `MODULE_SCOPE_REGISTRY` entries deleted + all `redirectPriority` lists rewritten to surviving modules; module-scope classifier stripped of research/full-launch special cases; run route zod enum + dispatch cases trimmed; questions/pin/timeline routes now landing-only; `/api/ventures` completedModules map → landing + shadowBoard.
- **DB/storage untouched:** `lib/queries.ts` `Conversation['module_id']` union + `CONVERSATION_MODULE_FALLBACK` + `VentureContext` legacy keys kept — they mirror the DB CHECK constraint and historical rows. Old ventures' research/branding context stays in JSONB (Co-pilot still reads it as context); no migrations needed.
- **Dashboard UI:** sidebar Build collapsible tree flattened to 3 direct module buttons (one click, no subgroup labels); cohorts fetch removed from layout load (one fewer request per dashboard boot); home Quick Actions + module chips → Landing/Shadow Board/Co-pilot; venture overview `VENTURE_TABS` → Landing + Shadow Board (default tab `landing`, context key mapping added for `shadowBoard`); module page: retired MODULES/SUGGESTIONS/NEXT_STEP_MAP/icons, marketing ConnectedChannelsPanel block, FullLaunchGraph, intensity selector, Research/Branding/Marketing/Feasibility/FullLaunch doc components all deleted (4502→~3700 lines); unknown/retired module slugs in URLs normalize to `landing`; reading panel restricted to landing (only module with a doc renderer); added a proper Shadow Board swords icon to `ModuleIconSvg` (was rendering null). `ResultCard` pruned of 5 retired displays + the now-orphaned recharts import (bundle win); `ModulePicker` → 3 modules. Settings "Default Research Depth" row removed. `manage` page venture links → `/landing`. Deleted `components/ui/FullLaunchGraph.tsx` + `AgentStatusRow.tsx`.
- **Efficiency:** `GET /api/ventures/[id]?module=<id>` now scopes the conversations payload to one module (module page uses it — was querying/shipping all 6 modules' histories per page load).
- **Public landing page rewritten** for the pivot: Hero stream lines + chips + subhead ("Any AI can write you a plan. Forze ships the parts that touch reality"); AgentGrid FEATURED card = Landing Page (was Full Launch), grid = Shadow Board/Co-pilot/Outreach/CRM/Investor Kit/Inspiration; OutputTabs → landing/shadow/investor tabs (retired previews deleted); HowItWorks step 2 → "Ship The Page, Face The Board"; Marquee + ComparisonTable rows updated; FAQ rewritten (positions against chatbots honestly); Testimonials quotes reworded; PricingSection plan copy; Footer tagline; root `layout.tsx` SEO metadata retargeted.
- **Co-pilot prompt updated:** it now *does* research/brand/GTM/financial thinking in chat instead of deferring to removed modules; suggests Landing Page → Shadow Board → Investor Kit/Outreach flow.
- Verification: `npx tsc --noEmit` exit 0; `npm run build` exit 0 (49 static pages, route table intact minus cohorts). Committed in granular commits.
- **Next:** live smoke-test a fresh venture (landing run without research/branding context, shadow board run, co-pilot chat, campaign draft gen); consider a proper Shadow Board reading-panel doc renderer; consider surfacing MVP Scalpel/Launch Autopilot UIs or deleting their backends.

### Previous Session (June 14 2026) — Main-Nav IDE Link + SEO (sitemap/robots) for `/ide`
- User: add the IDE link to the main navbar + "when I search Forze IDE the /ide page should come up — automatic or manual?"
- **`components/landing/Navbar.tsx`:** added an **`IDE`** nav item that routes to `/ide` (vs the existing scroll-to-section links). Replaced `scrollTo(id)` with `handleNavClick(link)` that branches on `link.href` (router.push) vs `link.id` (scrollIntoView); `navLinks` is now typed `{ label; id?; href? }[]`; map keys switched to `link.label`. Works in both desktop + mobile menus.
- **SEO discoverability (new files):** there was **no sitemap or robots** in the repo (only a GSC verification HTML in `public/`, so the domain is already verified in Google Search Console).
  - **`app/sitemap.ts` (new):** Next App Router sitemap listing public routes — `/`, **`/ide`** (priority 0.9), `/pricing`, `/blog`, legal pages. Auth-gated routes excluded. Uses `NEXT_PUBLIC_BASE_URL` (→ `https://forze.in`). Emits `/sitemap.xml`.
  - **`app/robots.ts` (new):** allow-all with `/dashboard/ /api/ /auth/ /investor/ /feedback/ /v/ /sites/` disallowed; references the sitemap. Emits `/robots.txt`.
- **SEO answer (told user):** not automatic/instant — Google must crawl + index the new page first (days–weeks); the sitemap + internal links (navbar + IdeBanner) speed discovery. Manual step only they can do: in Google Search Console, submit `https://forze.in/sitemap.xml` and use "URL Inspection → Request Indexing" on `https://forze.in/ide`. Branded query "forze ide" should rank once indexed since it's their own domain/brand.
- Verification: `npx tsc --noEmit` exit 0; `npm run build` exit 0 (`/sitemap.xml` + `/robots.txt` generated, `/ide` static).

### Previous Session (June 14 2026) — Forze IDE Promo Section on the Main Landing
- User: "put a section in the main landing that redirects to the IDE landing page." Added a desktop-app promo band to the **main** forze.in landing.
- **`components/landing/IdeBanner.tsx` (new):** a glass/gradient promo card (cool `#5A8CA5` accent, scan-line flourish) — eyebrow `FORZE IDE · Desktop app`, headline "Prefer to build in a real IDE?", short blurb, 4 highlight chips (Monaco editor / AI agent control room / Vercel deploys / Local-first · BYOK), a mini IDE window mock (activity rail + code lines + "agent: building" status), and two CTAs: **Explore Forze IDE →** routes to `/ide`, **Download** routes to `/download`. IntersectionObserver reveal, responsive 2-col → 1-col < 880px, `id="desktop"`. Uses existing warm design tokens.
- **`components/landing/LandingPage.tsx`:** imported `IdeBanner` and rendered it between `<ModuleShowcase />` and `<Testimonials />` (2-line surgical edit).
- Verification: `npx tsc --noEmit` exit 0; `npm run build` exit 0.

### Previous Session (June 14 2026) — `/download` Page Visual Redesign
- User: "make the download page look better." Redesigned **only** `components/download/DownloadClient.tsx` (the client UI) to match the Forze/IDE landing aesthetic. Untouched: `app/download/page.tsx` server gate, `requireAuth()`, the manifest fetch, OS-detection logic, the `/api/download/[platform]` links, and the coming-soon/disabled states — all preserved exactly.
- **What changed (pure visual):** replaced the plain Tailwind layout with the design-token system — ambient blob + dot-grid background, a header with the `FORZE [IDE]` lockup (links to `/ide`) + `ThemeToggle`, a glass version badge, gradient H1, and a big **OS-aware primary CTA** card (per-OS glyph + `Download for {label}` + `arch · ext`). "Other platforms" became hover-lift glass cards with per-OS accents (macOS `#8C7A5A`, Windows `#5A8CA5`, Linux `#5A8C6E`); coming-soon renders as a non-clickable muted card. Added trust chips (Local-first / BYOK / Signed / Free in beta), a "See everything Forze does →" link back to `/ide`, and copy aligned to the new "Sovereign OS" positioning. Responsive (grid collapses to 1 col < 520px).
- Verification: `npx tsc --noEmit` exit 0; `npm run build` exit 0 (`/download` still `ƒ` dynamic, auth-gated).

### Previous Session (June 14 2026) — `/ide` Content Rewrite to `landing-page-spec.md`
- User dropped `landing-page-spec.md` (a full content brief) and asked to update the landing page contents. The spec **re-positions the IDE**: it is **not** the venture-orchestrator workforce the first draft described — it's **"The Sovereign OS for Startup Founders,"** a real Tauri v2 desktop code editor (Monaco/Git/PTY terminals) wrapped in an AI control room (Agent Manager, Vibe Stations running Claude Code/Codex/Antigravity/OpenCode, a shared MCP context bus, the floating Assistant), plus a full Vercel client, kanban, dashboard/analytics, community, build-in-public, and local-first/BYOK sovereignty.
- **Design decision (asked the user):** offered full "Builder OS" matte-black + teal-cyan `#00d4ff` restyle vs. keeping the warm theme. User chose **keep the warm-amber theme** — so this was a *content/copy* rewrite only; zero color/token changes, still matches the main forze.in look.
- **Rewrote all copy + features** across the existing `components/ide-landing/` files; no design-system, agent, schema, API, or env changes:
  - `app/ide/page.tsx` — SEO retitled to spec Section 6 (`Forze IDE — The Sovereign OS for Startup Founders`) + new description/keywords/OG/Twitter.
  - `IdeHero.tsx` — new eyebrow (`Desktop · macOS / Windows / Linux`), H1 tagline, spec subhead, "Download for free" CTA; the mock window now shows a **real IDE** (activity bar · Monaco editor w/ git gutter + agent-prompt terminal · Assistant action stream) instead of the venture agents. Stat chips → 3 OS / local-first / BYOK.
  - `IdeShowcase.tsx` — 3 deep-dives rebuilt: **A real IDE** (editor/git/terminal/Prettier), **The AI control room** (Agent Manager + Vibe Stations grid visual + Context Bus + Assistant, with a keyless-Gemini/BYOK callout), **Sovereignty** (local-first/offline, BYOK, signed Tauri builds, cross-platform). Section ids now `editor` / `ai-control` / `sovereignty`.
  - `IdeCapabilities.tsx` — replaced the flat card grid with a **categorized full feature inventory** (6 category cards covering all ~34 features from spec Section 3: Editor & workbench, AI/agents/orchestration, Source control & safety, Deployments, Run the company, Platform & trust).
  - **New `IdeProblem.tsx`** — "One window. Not twelve tabs." band (scattered-tools → one Forze chip).
  - **New `IdeHowItWorks.tsx`** — 3 steps (download & open a folder → add keys / keyless default → build, ship, grow).
  - `IdePlatforms.tsx` — extensions per spec (macOS `.dmg`, Windows `.msi/.exe`, Linux `.AppImage/.deb`), trust chips → Local-first / BYOK / Signed / Free while in beta.
  - `IdeFaq.tsx` — 8 Q&As rewritten (free in beta, platforms, BYOK/keyless, privacy/local-first, vs VS Code/Cursor, which agents, Vercel deploy, lightweight/auto-update).
  - `IdeNav.tsx` nav links → Editor / AI agents / Features / Platforms / FAQ; `IdeLandingPage.tsx` composition reordered (Hero → Problem → Showcase → Capabilities → HowItWorks → Platforms → FAQ → CTA → Footer), final CTA → "Stop tab-switching. Start shipping." / "Download Forze — Free", footer copy → Sovereign-OS tagline + "Made for founders, by founders · forze.in".
- Verification: `npx tsc --noEmit` exit 0; `npm run build` exit 0 (`/ide` still `○` static prerender).

### Previous Session (June 13 2026) — Forze IDE Marketing Landing Page (`/ide`)
- User: "make a separate landing page for the new Forze IDE talking about all its capabilities, same style as the main landing page, on the same forze.in domain, looks really good, includes all features." Built a dedicated, self-contained marketing page at **`/ide`** that mirrors the main landing page's componentized architecture and design tokens exactly (warm-paper/obsidian theme, DM Sans + JetBrains Mono, glass cards, blob-float ambient, IntersectionObserver scroll reveals, `--accent` amber throughout).
- **Pure-additive, marketing-only.** No agent/schema/API/env changes. New route + a new `components/ide-landing/` folder. Reuses the existing `ThemeToggle`. CTAs route to the existing gated `/download` page; nav has a "Web app" link back to `/`.
- **`app/ide/page.tsx` (new):** server component with full SEO metadata (title/description/keywords, canonical `${BASE_URL}/ide`, OpenGraph + Twitter cards pointing at `/api/og`). Renders `<IdeLandingPage />`. Builds as `○ (Static)` — prerendered, ideal for a landing page.
- **`components/ide-landing/IdeLandingPage.tsx` (new):** composition — scroll progress bar + Nav → Hero → Capabilities → Showcase → Platforms → FAQ → final CTA → footer. The final CTA and a dedicated IDE footer (IDE/Forze/Legal columns + socials, all routing correctly — no dead scroll-to-id links) live inline here to keep file count sane.
- **`IdeNav.tsx`:** fixed glass-on-scroll nav, `FORZE [IDE]` lockup, section links (Capabilities/Workspace/Platforms/FAQ) with active-section IntersectionObserver, "Web app" link → `/`, Download CTA → `/download`, full mobile drawer.
- **`IdeHero.tsx`:** typewriter headline ("native./offline./instant./on your desktop."), animated mock IDE window (agent rail · streaming agent panel · live-preview pane), stat chips (~10 MB / 3 OS / auto-update). Reuses the `.hero-section` CSS class for the `100vh`/`100dvh` fallback.
- **`IdeCapabilities.tsx`:** 12-card grid covering every capability — full 11-agent workforce, ~10 MB Tauri/Rust native speed, live landing-page editor, real-time agent streams, ⌘K command palette, multi-venture command center, local-first/offline drafts, signed auto-updates, built-in CRM & outreach, Launch Autopilot & Routines, system-native notifications, secure-by-design. Module-palette accents for variety.
- **`IdeShowcase.tsx`:** 3 alternating deep-dive rows (ModuleShowcase pattern) with custom mock visuals — split-pane code editor + hot-reload preview, ⌘K command palette, and cross-platform installers + auto-update toast.
- **`IdePlatforms.tsx`:** Windows/macOS/Linux download cards (all → `/download`) + trust chips (~10 MB / Signed / Auto updates / Free) + "you'll sign in first" note.
- **`IdeFaq.tsx`:** 7-item accordion (free?, platforms, size, auto-update, account needed, vs web app, data security).
- Verification: `npx tsc --noEmit` exit 0; `npm run build` exit 0 (`/ide` prerendered static, full route table intact).
- **Next (optional):** add an "IDE / Desktop app" link into the main landing `Navbar`/`Footer` for discoverability (deliberately not touched this session to keep the edit surgical) — would satisfy item (4) from the download-wiring session below.

### Previous Session (June 13 2026) — Forze IDE Download + Updater Wiring
- User built the Forze IDE (a **Tauri** app, ~10 MB, in a separate repo) and wants the main platform to be where logged-in users download it. The two projects stay separate; they connect only through a single hosted `manifest.json` (installer URLs + signed update bundles) that the IDE CI publishes and the platform reads.
- **Decisions captured:** stack = Tauri; access = logged-in users only; hosting recommended = **Vercel Blob (private)** (10 MB makes egress a non-issue; native to stack); gate the *initial* download, leave the *update* feed public (the Tauri updater has no web session, and bundles are signature-verified).
- **`lib/ide-release.ts` (new):** types + reader for the combined manifest. Platform keys reuse Tauri's `{target}-{arch}` (`windows-x86_64`, `darwin-aarch64`, `darwin-x86_64`, `linux-x86_64`) so the download page and updater share one vocabulary. `getIdeManifest()` fetches `process.env.IDE_MANIFEST_URL` (60s revalidate); returns a STUB manifest when unset so everything runs locally with no external setup. Helpers: `isValidPlatformKey`, `isNewerVersion` (dotted-numeric), `IDE_PLATFORM_META`. Not `server-only` so the client UI can type-only-import the key/OS types.
- **`app/api/download/[platform]/route.ts` (new):** `requireAuth()` gate → resolve installer from manifest → 302 to bucket URL (browser downloads directly, platform never proxies bytes). On 401 redirects to `/signin?redirect=/download` (browser nav, not JSON). 503 when no build exists for the platform yet. TODO noted: swap raw URL for a short-lived signed URL in v1.5.
- **`app/api/update/[target]/[arch]/[current]/route.ts` (new):** public Tauri v2 updater feed. 204 when up-to-date / unknown platform / no signed bundle; else `{ version, pub_date, url, signature, notes }`. The IDE's `tauri.conf.json` points `plugins.updater.endpoints` at `…/api/update/{{target}}/{{arch}}/{{current_version}}`.
- **`components/download/DownloadClient.tsx` (new):** client UI, OS auto-detect via `userAgentData`/UA (macOS defaults primary CTA to Apple Silicon, Intel surfaced below — arch isn't reliably detectable from UA). Primary CTA + "other platforms" grid; "coming soon"/disabled when a platform has no build. Forze design tokens.
- **`app/download/page.tsx` (new):** server component; `getSession()` → redirect `/signin?redirect=/download` if unauthenticated; builds the platform item list and hands it to the client. Degrades to "coming soon" if the manifest fetch fails.
- **Env to provision:** `IDE_MANIFEST_URL` (the published manifest.json) — not set yet, so the page currently shows "coming soon". Vercel Blob store (→ `BLOB_READ_WRITE_TOKEN`) still to be created when CI starts uploading.
- Verification: `npx tsc --noEmit` exit 0. Pure-additive feature; no existing files touched, no schema/agent changes.
- **Next:** (1) user generates Tauri signing keys + wires `tauri.conf.json` endpoints; (2) I hand over the GitHub Actions release workflow (build → sign → upload to Blob → write manifest.json in the `IdeManifest` shape); (3) set `IDE_MANIFEST_URL`; (4) add a "Download" nav/CTA link into the landing/dashboard; (5) v1.5 hardening: signed download URLs + optional gated updates via per-user license token.

### Latest Session (June 5 2026) — LinkedIn Routine Channel
- User: "make routines work for LinkedIn as well." Routines previously supported only `gmail` and `instagram`. LinkedIn posting was already fully built in the marketing pipeline (`publishLinkedInAsset` in `lib/marketing-publish.ts`, content gen in `lib/linkedin-content-ai.ts`), so this was a wiring job, not new infrastructure.
- **`db/migrations/031_routines_linkedin.sql` (new):** (1) guarded `ALTER TYPE routine_channel ADD VALUE 'linkedin'` (same `pg_enum` existence-check pattern as 018). (2) Relaxed the `routine_channel_audience` CHECK from hard-coded `… OR channel = 'instagram'` to `… OR channel <> 'gmail'` so any social channel (instagram, linkedin, future) can run without a linked campaign while gmail still requires one.
- **`lib/schemas/routine.ts`:** added `'linkedin'` to `ROUTINE_CHANNELS`. The gmail-requires-campaign `superRefine` is unchanged, so LinkedIn needs no campaign.
- **`lib/routine-executor.ts`:** new `executeLinkedInRoutine` mirroring the Instagram branch — generates one fresh post via `generateFreshLinkedInDrafts(ventureName, marketing, research, 1, seed)` (note: LinkedIn gen also takes `research` from `context.research`, unlike IG), inserts a pre-approved `linkedin`/`linkedin_post` marketing asset, then queues + dispatches a single publish job inline (`dispatchDuePublishJobs({ jobIds: [job.id] })`) so one routine tick = one published post. Added explicit `else if (routine.channel === 'linkedin')` branch in `executeRoutine` (was falling through to the IG `else`).
- **`components/venture/RoutinesPanel.tsx`:** added `linkedin: 'LinkedIn'` to `CHANNEL_LABEL` and a third `ChannelChip` in the create form. Campaign picker / submit-disable logic is gmail-gated already, so LinkedIn needs nothing there.
- Reused existing dispatch + reauth handling — failures surface `last_error` (e.g. "LinkedIn needs to be reconnected") onto the routine_runs row exactly like IG.
- Verification: `npx tsc --noEmit` exit 0. **Migration 031 must be applied to the DB before a LinkedIn routine can be created** (enum value + relaxed constraint).
- **Next:** apply migration 031; smoke-test creating a LinkedIn routine and firing it via "Run due now" against a connected LinkedIn account.

### Earlier Session (June 2 2026) — Gemini 3 Pro Model Fix
- **Bug:** Architect/Feasibility runs crashed with `404 — models/gemini-3-pro-preview is no longer available`. Google discontinued `gemini-3-pro-preview` on March 26, 2026; the replacement is `gemini-3.1-pro-preview` (confirmed via web search).
- **Fix:** Updated all Pro-model references from `models/gemini-3-pro-preview` → `models/gemini-3.1-pro-preview`:
  - `lib/gemini.ts` — `getProModelWithThinking` default + `getProModelWithSearchAndThinking` (Grok-fallback path and Gemini path).
  - `agents/orchestrator.ts` — explicit `getProModelWithThinking(5000, ...)` call for the Architect step.
- Flash model (`gemini-3-flash-preview`) was NOT erroring in logs and is still serving, so it was left untouched.
- Grok 403 (xAI credit/spend limit) left as-is per user instruction — Gemini fallback handles it.
- **Next:** verify a live Full Launch run completes end-to-end now that the Pro model resolves.

### Earlier Session (June 2 2026) — Full Launch Workflow Graph
- User wanted the Full Launch live progress to render as a connected workflow graph (nodes + lines + glow) instead of the flat stack of `AgentStatusRow` rectangles.
- **New file `components/ui/FullLaunchGraph.tsx`:** self-contained graph that lays out the four Full Launch agents in their real dependency order — Genesis → Identity → (Pipeline + Feasibility in parallel, per the CLAUDE.md launch order; Marketing stays excluded). Nodes positioned in a `0–100` coordinate space; an absolutely-positioned `<svg viewBox="0 0 100 100" preserveAspectRatio="none">` draws the connecting edges *behind* the node cards. Lines use `vectorEffect="non-scaling-stroke"` so stroke width stays crisp under the non-uniform viewBox scaling, and glow comes from screen-space `drop-shadow(...)` filters (not feGaussianBlur, which would distort under the same scaling).
- **Edge states:** idle (source pending/failed → dim `--border-strong` track), active (source running/complete but target unfinished → accent track + flowing dashed `motion.path` animating `strokeDashoffset` with a glow), done (both endpoints complete → solid bright accent + steady glow). Edge color keys off the *target* node accent so the "energy" reads as flowing into the next agent.
- **Node cards:** icon disc + name + role + status chip (Queued / Running / Done / Failed). Running nodes get a pulsing box-shadow ring + scaling icon; complete nodes get a steady soft glow. Colors match the module accents (Research `#5A8C6E`, Branding `#5A6E8C`, Landing `#8C7A5A`, Feasibility `#7A5A8C`). Uses existing design tokens (`--card-solid`, `--border`, `--nav-active`, `--muted`, `--text`).
- **Wiring in `app/dashboard/venture/[id]/[module]/page.tsx`:** replaced the `FULL_LAUNCH_AGENTS.map(... <AgentStatusRow/>)` block (~line 1620) with `<FullLaunchGraph statuses={{ genesis, identity, pipeline, feasibility }} />`, mapping each from `entry.agentStatuses['research'|'branding'|'landing'|'feasibility'].status` (defaulting to `'pending'`). Removed the now-unused `AgentStatusRow` import. `FULL_LAUNCH_AGENTS` is still used by the status-init helpers so it stayed.
- `AgentStatusRow.tsx` is left in place (no other importers right now, but kept as a reusable primitive).
- Verification: `npx tsc --noEmit` exit 0. No schema, API, agent, or env changes — pure UI.

### Latest Session (May 28 2026) — LinkedIn Auto-Generated Post Images
- User reported: text-only LinkedIn posts get demoted by the algorithm; wants images attached automatically (matching the existing Instagram behaviour). Scope: a single surgical change in `lib/marketing-publish.ts`. No new files, no schema changes, no UI changes — the LinkedIn payload type already carried `imageUrl`/`brandColors`/`ventureName` from the Instagram path; nothing to migrate.
- **What was missing:** `publishLinkedInAsset` only set `shareMediaCategory: 'ARTICLE'` (when `payload.linkUrl` was supplied) or `'NONE'`. Never `'IMAGE'`. LinkedIn requires the v2 assets two-step dance — `POST /v2/assets?action=registerUpload` returns an upload URL + asset URN (`urn:li:digitalmediaAsset:...`); PUT the raw image bytes to that URL with the same bearer token; reference the URN in the ugcPost `media[]` with `shareMediaCategory: 'IMAGE'`.
- **Two new helpers added above `publishLinkedInAsset`:**
  - `uploadLinkedInImage({ accessToken, ownerUrn, imageBytes, contentType })` — runs registerUpload with `recipes: ['urn:li:digitalmediaRecipe:feedshare-image']` + `owner: ownerUrn` + `serviceRelationships: [{ relationshipType: 'OWNER', identifier: 'urn:li:userGeneratedContent' }]`, parses out `value.uploadMechanism['com.linkedin.digitalmedia.uploading.MediaUploadHttpRequest'].uploadUrl` and `value.asset`, then PUTs the bytes (as `new Uint8Array(buffer)` because native fetch's BodyInit type rejects raw Node Buffer in this lib.dom.d.ts version). Throws `MarketingProviderError` with proper retryable flag on 5xx/429.
  - `resolveLinkedInImageAsset({ accessToken, ownerUrn, asset, payload })` — opt-out via `payload.skipImage === true`; otherwise reuses `payload.imageUrl` if set or calls `generatePostImage(asset.body, ventureName, brandColors)` from `lib/marketing-image-gen.ts` (same generator Instagram uses → 1080×1080 JPEG to Supabase storage). Fetches the URL, validates `content-type` starts with `image/` and bytes > 0, hands off to `uploadLinkedInImage`. **Wrapped in try-catch returning `null` on any failure** so a transient image-gen error never kills an otherwise-working text post — graceful degradation back to the old ARTICLE-or-NONE path.
- **`publishLinkedInAsset` rewired:** computes `imageAssetUrn` first via the new resolver, then picks shareMediaCategory in priority order IMAGE > ARTICLE > NONE. Image media entry uses `{ status: 'READY', media: imageAssetUrn, title: { text: asset.title || 'Post image' }, description: { text: asset.title || '' } }` per LinkedIn's UGC schema. Existing ARTICLE branch preserved as the fallback when image gen fails AND `linkUrl` is supplied. NONE branch unchanged. Return metadata now includes `imageAssetUrn` alongside the existing `restliId`, so the asset row's `metadata` JSONB carries the URN for any future analytics dashboards that want to key off it.
- **Why prefer image over link card:** LinkedIn only allows ONE media category per ugcPost. Native images get the biggest algorithmic boost (per LinkedInRocket playbook + the user's explicit request); link cards are a distant second. If a user wants a link card preserved they can set `payload.skipImage: true` — the opt-out is documented in code via the `payload.skipImage === true` branch in `resolveLinkedInImageAsset`.
- **Cost note:** every LinkedIn publish now runs one Gemini image gen (~$0.04 per call on gemini-2.5-flash-image) unless `payload.imageUrl` was supplied at draft time. Same per-post cost the Instagram path has been paying since Phase 11. Studio + Pro plans absorb this trivially; Free + Starter don't have outreach/marketing publish access at all (see Phase 18 gating) so no impact on the cheaper tiers.
- Verification: `npx tsc --noEmit -p .` exit 0. No new env vars (reuses `IMAGE_GEMINI_API_KEY` / `GEMINI_API_KEY`). No new dependencies. No DB changes. No UI changes — existing ConnectedChannelsPanel "Publish" button now silently produces image-attached posts.
- **What still needs the LinkedIn-app side:** nothing new. The `w_member_social` scope already grants access to `/v2/assets?action=registerUpload` and `/v2/ugcPosts` with `shareMediaCategory: 'IMAGE'`. Same auth surface as before. If the MDP `Share on LinkedIn` product hasn't been approved on the dev's LinkedIn app yet (see May 25 session), publish still 403s — but that's pre-existing, not caused by this change.

### Previous Session (May 25 2026) — LinkedIn Integration Audit + Permalink Fix
- Audited the LinkedIn integration end-to-end after a user request to "finish everything for the linkedin integration". Finding: LinkedIn was **already fully built** by prior sessions (it rides on the generic `[provider]` machinery alongside YouTube + Instagram). Concretely already shipped: OAuth connect/callback/disconnect (`app/api/integrations/[provider]/{connect,callback,disconnect}/route.ts`); scopes `openid profile email w_member_social` in `lib/marketing-oauth.ts` `LINKEDIN_SCOPES`; auth URL builder + token exchange + `/v2/userinfo` profile fetch; refresh-token rotation in `lib/marketing-publish.ts` `refreshLinkedInAccessToken`; UGC post publishing via `publishLinkedInAsset` → POST `/v2/ugcPosts` with text + optional ARTICLE share; draft generation from marketing brief in `lib/marketing-drafts.ts` `buildLinkedInDraftSeeds`; full UI in `components/marketing/ConnectedChannelsPanel.tsx` with per-provider connect/disconnect/Create LinkedIn Drafts/edit/approve/schedule/publish/cancel; token encryption at rest via `lib/marketing-crypto.ts` (AES-256-GCM keyed off `MARKETING_TOKEN_ENCRYPTION_KEY`); reauth_required state flow on 401/403.
- **The one real gap:** `publishLinkedInAsset` was returning `permalink: null` even though LinkedIn returns the share URN in the `X-Restli-Id` response header (e.g. `urn:li:share:7234567890123456789` or `urn:li:ugcPost:7...`). UI consequence: after publishing a LinkedIn draft, the "Open published asset" link in `ConnectedChannelsPanel` was suppressed because the asset row had no permalink. **Fix:** construct `https://www.linkedin.com/feed/update/${encodeURIComponent(restliId)}/` from the URN. The `encodeURIComponent` is mandatory — LinkedIn's router treats raw `urn:li:share:` as multiple path segments and 404s, only the URL-encoded form (`urn%3Ali%3Ashare%3A...`) resolves to the actual post. Existing `metadata.restliId` field preserved for forward compatibility (analytics dashboards that already key off it keep working).
- **Cleanup:** removed two dead env vars I had added earlier in the session — `LINKEDIN_REDIRECT_URI` and `LINKEDIN_TOKEN_ENCRYPTION_KEY`. Neither is read anywhere: redirect URI is computed per-request from `request.nextUrl.origin` in the connect route, and token encryption goes through the shared `MARKETING_TOKEN_ENCRYPTION_KEY` via `lib/marketing-crypto.ts`. Replaced with a documentation comment block in `.env.local` so a future founder doesn't reintroduce them, and listing both the dev (`https://localhost:3000/api/integrations/linkedin/callback`) and prod (`https://forze.in/api/integrations/linkedin/callback`) redirect URLs that must be registered in the LinkedIn app's Authorized Redirect URLs dashboard.
- **What's required on the LinkedIn-app side** (one-time setup, no code): create the LinkedIn app at linkedin.com/developers (requires a verified Company Page), request the "Sign In with LinkedIn using OpenID Connect" product (auto-approved), the "Share on LinkedIn" product (`w_member_social` — requires MDP application, 2–6 wk review), and "Refresh Tokens" (auto-approved). The MDP pitch for Forze: "Users generate marketing content and publish to their own LinkedIn with one click. We do not automate posting, do not access connections, do not send messages." That's a textbook approval. Until MDP approval lands the OAuth flow + token exchange + drafts UI all work, only the final `/v2/ugcPosts` POST returns 403 until the scope is granted — which the publish dispatcher already correctly handles by marking `connection.status = 'reauth_required'`.
- Verification: `npx tsc --noEmit -p .` exit 0 after the publish.ts change. No new files, no migrations needed, no schema changes. Two surgical commits — the permalink construction in `marketing-publish.ts`, and the `.env.local` cleanup.

### Previous Session (May 22 2026) — Per-Plan Feature Gating, Weekly Credit Refresh, Per-Week Action Ceilings
- Built the user-defined tiered access model end-to-end. Before this session, every plan got every module and the only abuse cap was credit count; CRM, Inspiration, and "Outreach" (campaigns + direct-mail + launch-autopilot) had no plan gating at all and credits refreshed monthly. After: per-plan `allowedModules` + `allowedFeatures`, weekly credit grant that resets every Monday 00:00 IST, per-week ceilings on the three un-credited gated surfaces. Free + Starter blocked from Outreach/CRM/Inspiration; Builder/Pro/Studio unlocked. Existing builder+ users see no behaviour change.
- **Locked spec** (with user):
  - Free 10/wk · Starter 20/wk · Builder 60/wk · Pro 300/wk · Studio 600/wk
  - Outreach + CRM + Inspiration unlocked on Builder+
  - Builder action ceilings: 20 inspiration analyses / 50 CRM emails / 3 campaign sends per week
  - Pro: 75 / 250 / 15 per week
  - Studio: 300 / unlimited / unlimited per week
  - Top-ups (`topup-50`, `topup-175`) never expire — live in a separate ledger pool
  - All resets fire at Monday 00:00 IST (Asia/Kolkata, no DST so hardcoded UTC+5:30)
- **Migration `030_weekly_credits_and_feature_gates.sql`.** Widens `credit_ledger.kind` CHECK to add `weekly_grant` (the per-week positive entry) and `weekly_expiry` (the negative drain of last week's non-topup balance). Adds nullable `users.weekly_credit_period_start TIMESTAMPTZ` column anchoring the start of the user's current weekly window (partial index `WHERE NOT NULL` for cheap active-user dashboards). Creates `feature_usage_counters (user_id, feature_id, period_start, count)` composite-PK table — feature_id is CHECK-constrained to `inspiration_analyze` / `crm_email_send` / `campaign_send` (matches the new ActionId type), period_start anchors each row to a specific Monday-IST window, count is the running tally. RLS on the new table is `auth.uid() = user_id` for SELECT/INSERT/UPDATE. Reuses `update_billing_updated_at()` trigger from migration 007. Every change is additive + IF NOT EXISTS / IF EXISTS guarded so the migration is safely re-runnable.
- **`lib/billing.ts`.** New types: `FeatureId` (`'crm' | 'inspiration' | 'outreach'`), `ActionId` (`'inspiration_analyze' | 'crm_email_send' | 'campaign_send'`), `WeeklyActionLimits` (per-action numeric ceilings). New constants: `ALL_FEATURES`, `CORE_BILLING_MODULES` (= `ALL_BILLING_MODULES` minus `launch-autopilot` — that's the set Free + Starter get), `UNLIMITED_WEEKLY_ACTION_LIMIT = 1_000_000` (the "monitor but don't enforce" sentinel for Studio's CRM emails + campaign sends). `BillingPlan` gains `weeklyCredits`, `allowedFeatures`, `weeklyActionLimits` alongside the preserved `monthlyCredits` (= weeklyCredits × 4 so legacy `subscriptions.credits_per_cycle` + razorpay metadata still receive sensible numbers). New helpers: `isFeatureIncluded`, `getWeeklyActionLimit`, `getWeeklyCredits`, `getCurrentWeeklyPeriodStart` (Mon-00:00-IST anchor via IST-clock-shift + Monday-snap + back-to-UTC), `getCurrentWeeklyPeriodEnd` (+7 days). `FEATURE_LABELS` for UI strings, `ACTION_TO_FEATURE` so the queries layer can resolve an action's parent feature without conditionals.
- **`lib/billing-queries.ts`.** `BillingSnapshot` interface grows seven new fields (`allowedFeatures`, `weeklyCredits`, `weeklyActionLimits`, `weeklyActionUsage`, `weeklyPeriodStart`, `weeklyPeriodEnd`) — every existing field preserved with identical shape so legacy consumers compile unchanged. New `refreshWeeklyCreditsIfDue(userId, planSlug, db)` fires inside `getBillingSnapshot` for all non-unlimited users: reads `users.weekly_credit_period_start`, short-circuits if same as current Mon-IST anchor; otherwise (1) sums `SUM(credit_ledger.credits WHERE kind != 'topup')` to get the expiring pool, (2) if positive inserts a `weekly_expiry` ledger row with `credits = -nonTopupBalance`, (3) inserts a fresh `weekly_grant` for the new week with `credits = plan.weeklyCredits`, (4) updates the anchor column. Each step has its own try-catch + warn-and-bail so partial failures leave previous balance intact rather than half-resetting. Old `grantFreeCreditsIfNeeded` removed — the refresh path subsumes initial seeding (NULL anchor unconditionally triggers a refresh). `assertCanRunModule` now ALSO checks `isModuleIncluded(planSlug, moduleId)` and throws 403 `module_not_in_plan` — that's what locks `launch-autopilot` out of Free + Starter without touching agents/launch-autopilot.ts. New `assertCanAccessFeature(userId, featureId)` for the un-credited surfaces, throws 403 `feature_not_in_plan`. New `assertCanPerformAction(userId, actionId)` composite gate that (1) calls assertCanAccessFeature for the parent feature, (2) reads `getWeeklyActionLimit`, (3) compares against snapshot.weeklyActionUsage, (4) throws 429 `weekly_action_limit_reached` with friendly "Resets Monday, May 26, 12:00 AM IST" message, (5) read-modify-write upserts the feature_usage_counters row. Unlimited-override + Studio-tier UNLIMITED_WEEKLY_ACTION_LIMIT plans skip the limit but still increment for observability. Concurrent racers converge on the same composite-PK row; worst-case race lets one extra action through — acceptable for abuse-prevention semantics.
- **`lib/billing-http.ts` (new, 38 LOC).** Thin Next-aware adapter so each gated route's diff is exactly two lines. `gateFeatureForResponse(userId, featureId)` and `gateActionForResponse(userId, actionId)` both return `GateResult = { ok: true, snapshot } | { ok: false, response: NextResponse }`. BillingError caught and converted to `{ error, code }` body with status set from `err.status`. Non-BillingError failures rethrow into the route's existing catch (generic 500). Kept in a separate file because billing-queries.ts is otherwise free of Next.js types.
- **Route gating across 22 endpoints, no logic changes.** Insertion pattern: right after auth resolution (`requireAuth` / `getSession` / `requireMarketingVenture`), before any business-logic call. CRM (8 routes gated with `'crm'`): inbox, leads, leads/email, leads/[leadId] PATCH+DELETE, leads/export, replies, analytics, dispatch (the last also gets a per-recipient `crm_email_send` gate inside the send loop that breaks early on 429 — preserves partial sends + returns sentCount + errors so the founder sees exactly where they hit the wall). Inspiration (5 routes gated with `'inspiration'`): list, analyze (uses gateActionForResponse('inspiration_analyze') which checks both the feature AND the per-week ceiling in one call), [analysisId] GET+PATCH+DELETE, refine, apply POST+DELETE. Outreach (9 routes gated with `'outreach'`): campaigns GET+POST, [id] GET+PATCH+DELETE, [id]/analytics, [id]/leads GET+POST, [id]/direct-leads, [id]/generate-email, [id]/poll-replies GET+POST, [id]/send (uses gateActionForResponse('campaign_send') at top — 1 invocation = 1 counter tick, matches the user-visible "campaign sends / week" pricing string), and ventures/[id]/direct-mail/generate-email. The previously-broken launch-autopilot module gate is already enforced via the tightened assertCanRunModule. Public/un-authed routes (blog, investor-kit lookups, feedback submission, tracking pixels, OAuth callbacks, cron tick, /v/[id] + /sites/[subdomain] previews) deliberately left ungated — none touch the gated features.
- **`app/pricing/page.tsx`.** Hero subtitle pivots from "Every plan includes every module" to "Outreach, CRM, and Inspiration unlock on Builder and above — Free + Starter focus on validation. Top-ups never expire." Per-plan cards now show `{plan.weeklyCredits} credits / week` (was `monthlyCredits/month`), and replace "All 11 modules included" with either "Outreach + CRM + Inspiration unlocked" or "Validation modules only" depending on `plan.allowedFeatures.length === ALL_FEATURES.length`. Builder + Pro cards get a fourth line listing the per-week campaign-send ceiling. New full-width per-plan availability matrix table (6 rows × 5 plan columns) using `isFeatureIncluded` + `isModuleIncluded` — cells render ✓ in accent color or em-dash in muted color, ceiling rows render raw integer or ∞ for UNLIMITED. Studio venture-limit string now reads "Unlimited active ventures" instead of the raw `1000000` sentinel. Three new CSSProperties helpers (tableHeadStyle, tableCellLabelStyle, tableCellStyle).
- **`components/landing/PricingSection.tsx`.** Public-landing PLAN_HIGHLIGHTS arrays updated to weekly numbers (10/20/60/300/600). Free + Starter bullet 3 says "Validation modules only", Builder+ says "Outreach, CRM, Inspiration unlocked" + a fourth bullet with the action ceilings. Studio gets "Unlimited campaign + CRM sends". PLAN_DESCRIPTIONS for Builder rewritten: "Outreach, CRM, and Inspiration unlock — go from idea to launched venture." Hero subtitle now says "Credits refresh every Monday 00:00 IST. Outreach, CRM, and Inspiration unlock on Builder and up. Top-ups never expire." maxWidth bumped 400→460 to accommodate longer copy. Card structure (price toggle, animations, particle effects, CTA wiring) untouched.
- **`components/billing/BillingPanel.tsx`.** Client-side BillingSnapshot interface extended with the seven new fields so the new /api/billing/me payload decodes cleanly. Hero metric cards: "Monthly credits" → "Weekly credits", "Modules" (was always "All modules") → "Premium features" (shows "All unlocked" / "Locked" / comma-joined list). Hero strip now shows BOTH "Weekly credits reset Sat, May 25, 12:00 AM IST" AND "Subscription renews on <date>" — two date lines, weekly ticks faster than the subscription cycle so both matter. New "This week so far" glass-card section (only rendered when allowedFeatures.length > 0 so free/starter don't see all-blocked bars). Three UsageBar rows for Inspiration analyses / CRM emails / Campaign sends, each tabular-nums fraction + 6px progress bar color-coded accent → amber (≥80%) → crimson (at cap). Unlimited rows render `∞`, locked rows render "Not in your plan". Top-ups header gains a one-liner: "Top-ups never expire. They live in a separate pool from your weekly credits and survive every Monday reset." New UsageBar component defined right next to MetricCard.
- Verification: `npx tsc --noEmit -p .` exit 0 after every commit. Full `npm run build` exit 0 with the ~100-route table re-registered, no regressions. Shipped as 7 surgical commits (migration, billing.ts, billing-queries.ts, route gating + http helper, /pricing, landing PricingSection, BillingPanel). Migration 030 must be applied to Supabase before the new ledger kinds + anchor column + counters table can persist; until then the refresh path try-catches every DB op so an unapplied migration degrades to "no refresh happens" rather than blocking reads.
- Behavioural guarantees preserved: existing Builder+ users see no change (still have outreach/CRM/inspiration). Existing free users LOSE access to those three (per spec) but see a clean 403 with "feature_not_in_plan" code + friendly upgrade message instead of a 404. Existing credit balances preserved at migration time — next Monday's natural refresh rolls them over. Top-ups (kind=topup) survive every weekly drain. Unlimited-override emails bypass everything. Hourly rate-limit + Zod + RLS all unchanged.

### Previous Session (May 21 2026, overnight) — User-Supplied Images for the Landing Module
- Founders can now upload their own images (logo, hero photo, product screenshots, team portraits, customer-trust logos, etc.) and the Production Pipeline agent embeds them in the generated landing page with the actual Supabase Storage URLs instead of hallucinating stock-photo links. Works on both initial generation and edit-mode ("swap the hero", "add my logo to the navbar", "use my screenshot in the features section").
- **Migration `029_landing_assets.sql`.** New `landing_assets` table — `id`, `venture_id` (FK → ventures, ON DELETE CASCADE), `user_id` (FK → auth.users), `storage_path`, `public_url`, `label`, `alt_text`, `kind` (CHECK in 'logo' | 'hero' | 'product' | 'team' | 'customer-logo' | 'background' | 'testimonial' | 'feature' | 'image'), `mime_type`, `byte_size`, `width`, `height`, `created_at`, `updated_at`. Indexed by `(venture_id, created_at DESC)` and `(user_id, created_at DESC)`. RLS enabled with `landing_assets_select_member` / `insert_editor` / `update_editor` / `delete_editor` policies that gate on the `venture_members` table (migration 024) with a legacy fallback to `ventures.user_id` so old single-owner ventures still work without enrollment. `updated_at` trigger mirrors the 027 inspiration pattern.
- **Storage helper `lib/storage/landing-assets-storage.ts`.** Mirrors `lib/marketing-image-gen.ts` Instagram-bucket pattern. New public Supabase Storage bucket `landing-assets` (override via `LANDING_ASSETS_BUCKET` env). 8 MB default limit (`LANDING_ASSETS_MAX_BYTES`). Allowed MIME: `image/jpeg`, `image/png`, `image/webp`, `image/svg+xml`, `image/gif`. `ensureLandingAssetsBucket` lazy-creates/updates the bucket once per process via `bucketReady` promise cache. `uploadLandingAssetToStorage` writes the binary at path `{ventureId}/{uploadId-8chars}-{safeFilename}.{ext}` with `cacheControl: '31536000'` (1y) and `upsert: false`, then returns the `getPublicUrl` direct URL. `deleteLandingAssetFromStorage` soft-fails on "not found" so deletes stay idempotent.
- **Schemas `lib/schemas/landing-assets.ts`.** `LandingAssetKindSchema` (the 9-value enum above), `LandingAssetSchema` (the full row in camelCase — client-facing shape), `LandingAssetUploadMetaSchema` (POST multipart fields — label 0-160 chars, altText 0-280 chars, kind defaults to 'image'), `LandingAssetPatchSchema` (PATCH partial edits with a refine() that requires at least one of label/altText/kind).
- **Query helpers `lib/queries/landing-asset-queries.ts`.** `listLandingAssets(ventureId)` — RLS-gated SELECT ordered by `created_at ASC` so prompt slot ordering stays stable across regenerations. `insertLandingAsset(input)` — tries RLS first, falls back to admin client (legacy ventures pattern from inspiration-queries.ts). `getLandingAssetById`, `updateLandingAsset(ventureId, assetId, patch)` — partial column update only, returns null on not-found. `deleteLandingAssetRow` — fetches the storage_path before delete so the route can clean storage after the row is gone, with admin-client fallback. Private `rowToAsset` transforms snake_case → camelCase.
- **API routes.** `POST /api/ventures/[id]/assets` (`app/api/ventures/[id]/assets/route.ts`) — multipart upload. Validates UUID ventureId, `getVentureAccess` (viewers blocked), file type/size, parses Zod metadata. Generates a random 8-char `uploadId`, uploads to storage, inserts the row, returns `{ asset }`. `GET /api/ventures/[id]/assets` — lists assets + the size/MIME limits so the UI can show "Max 8 MB, PNG/JPG/WEBP/SVG/GIF" without hardcoding. `PATCH /api/ventures/[id]/assets/[assetId]` — partial metadata edit (label/alt/kind). `DELETE /api/ventures/[id]/assets/[assetId]` — removes the row then best-effort removes the storage object; row deletion is the canonical action so a storage cleanup failure logs a warning but returns 200.
- **Pipeline agent integration (`agents/pipeline.ts`).** New `buildLandingAssetsBlock(rawAssets)` helper that defensively filters the array (skip rows missing `publicUrl`), sorts by `kind` priority (logo → hero → background → product → feature → team → testimonial → customer-logo → image) then by `createdAt`, caps at 24 assets, and emits an "## User-Supplied Images (REQUIRED — use these exact URLs)" markdown block. The block includes per-kind placement guidance (logos in nav+footer; hero behind/beside headline; background as background-image; product in features/hero; etc.), implementation rules (`<img src="EXACT_URL_FROM_BELOW" alt="EXACT_ALT_FROM_BELOW" loading="lazy" />` for foreground, `style={{ backgroundImage: "url(EXACT_URL_FROM_BELOW)" }}` for backgrounds, never reference an asset not in the list, never use Unsplash/Pexels/placeholder/picsum URLs), and a numbered list of every asset with its kind, label, dimensions, URL, and ALT. The block is appended to `contextParts` near the end so it sits closest to the generation directive (LLMs remember end-of-context content best). Edit mode gets the same block, plus an extra clause: "If the founder's request involves ADDING, SWAPPING, or REMOVING any image, you MUST output a fullComponent (not a copy-only patch) so the <img> / background-image references actually change."
- **Run-route wiring (`app/api/ventures/[id]/run/route.ts`).** New `listLandingAssets` import. In the `landing` and `full-launch` branches, before the agent runs, fetches the asset list and attaches it to `ventureInput.context.landingAssets`. Wrapped in try/catch so an asset-table failure (e.g. migration 029 not applied) logs and degrades to text-only generation — the agent runs unchanged in that case.
- **UI — `components/venture/LandingAssetsPopover.tsx`.** A 420×540 popover anchored above the landing-module chat input. Top: header with thumbnail-icon, asset count, "Max X MB each" hint, close ×. Body: error banner (if any), upload zone (drag/file picker → stage → label/altText/kind fields → Upload button), and the existing asset list (44px thumb, label, byte size, inline kind select, delete button). Local `URL.createObjectURL` preview lets the founder review before upload; `revokeObjectURL` runs on unmount. Auto-fills the label from the basename when blank ("acme-logo.png" → "Acme logo"). Delete confirms first ("removed from any landing page that references it"). Inline kind PATCH lets the founder relabel after upload without re-uploading. Footer reminds: "Next time you run or edit the Landing module, the agent will embed these images automatically."
- **Mount point (`app/dashboard/venture/[id]/[module]/page.tsx`).** New `assetsOpen` state, popover mounted ONLY when `activeModule === 'landing'` to keep other modules' chat input minimal. A 28px "Images" button sits to the left of the `⌘+Enter to run` keyboard hint, with a faint vertical separator. The button toggles an absolute-positioned `motion.div` (slide-up animation via framer-motion) containing the popover. A click-away overlay at `zIndex: 25` (below the popover's `zIndex: 30`) dismisses on outside click. The popover's own `onClose` × also dismisses.
- Verification: `npx tsc --noEmit -p .` exit 0. Migration 029 needs to be applied to Supabase before uploads can succeed in production; the run-route fetch is try/catch wrapped so an unapplied migration degrades to text-only generation rather than blocking. The `landing-assets` Storage bucket is lazy-created on first upload, no manual setup needed. The agent prompts now have THREE images-related guidance layers stacking: (1) the runtime-safety prompt (no icon libraries), (2) the inspiration directive (visual feel + ground-truth colors), and (3) the user-asset directive (exact URLs for real images) — and the user-asset block is the highest priority because it ships actual URLs the LLM must verbatim.

### Latest Session (May 21 2026, late night) — Landing Module Error Fixing & Debugging Layer
- Built on top of the runtime safety shim (previous session) so the Landing module is now *self-diagnosing*. Goal: when the LLM drifts or the founder's edit instruction produces broken code, the system surfaces the exact problem with an actionable next step instead of just rendering a blank iframe. Four layers, each independently useful: (1) a static validator that scans the generated component pre-persist, (2) a React error boundary that catches runtime render errors, (3) global script + promise listeners that catch everything else, and (4) a dashboard banner that surfaces all of the above with a one-click "Copy fix prompt" action.
- **Static validator (`lib/landing-page.ts` new export `validateLandingComponent`).** Pure function over the generated component string. Returns `{ issues, sanitized, hasErrors }`. Rule catalogue (severity-tagged): forbidden imports — `lucide-react`, `@heroicons/*`, `react-icons/*`, `@tabler/icons`, `phosphor-icons`, `feather-icons`, `@fortawesome/*`, `framer-motion`, `next/{image,link,router,navigation,head,script,font,dynamic}`, `clsx` / `classnames` / `tailwind-merge`, `react-router` — and a catch-all `^import … from '…'` that strips any other ES module imports because Babel-standalone in script mode doesn't resolve specifiers; runtime hazards — `process.env.X` (replaced with `""`), `eval(`, `new Function(`, `document.write(`; drift detection — `<LucideIcons.X>`, `<motion.tag>`, `<AnimatePresence>` (these are caught by the shim, but reported so the founder knows the visual didn't match the requested icon). All import rules ship with a `replacement` that auto-comments-out the offending line; `process.env.X` substitutions ship a string literal so the component keeps parsing. Drift-detection rules report only (sanitization there would change the visual output, which is the wrong tradeoff). The validator also runs a single-pass `checkBalance(code)` over braces / parens / brackets — skipping strings (single, double, template) and `//` + `/* */` comments — and reports any imbalance as a hard error because Babel will crash silently on a mismatch and show a blank iframe.
- **Pipeline integration (`agents/pipeline.ts`).** New `runComponentValidator(component, onStream, contextLabel)` helper called immediately after `resolveLandingComponent(...)` in BOTH branches — initial generation and edit mode — so every component that reaches the iframe has been linted. Findings stream to the founder as readable lines: `[Initial generation] Component validator: 1 warning, 1 info — auto-fixed where safe.` followed by per-issue rows with severity glyph (`✗`/`!`/`·`), message, optional `(line N)`, and `[auto-fixed]` tag where applicable. First 12 issues stream; remainder summarised. The sanitized component string replaces `validated.fullComponent` before persistence, so the DB never holds imports that Babel will strip awkwardly mid-render or `process.env.X` references that throw `ReferenceError`. Caller can branch on `hasErrors` later if we want to gate publish on a clean validator pass; for now we report and let it through.
- **Error boundary in the runtime shim (`lib/landing-page.ts` `LANDING_RUNTIME_SHIM` extension).** Defines `window.__ForzeErrorBoundary__` — a class extending `React.Component` (via ES5 prototype chain so it parses in the plain `<script>` block; the shim runs before Babel, so no JSX or class-syntax). Implements `getDerivedStateFromError` + `componentDidCatch`. On catch: (a) postMessage `forze:landing-error` to `window.parent` with `{ kind: 'react', message, stack, componentStack }` (truncated to 4KB / 2KB to stay under postMessage size budget), (b) `console.error('[Forze landing] React error caught:', error, info)` for in-iframe devtools visibility, (c) re-renders with a styled dark-mode error screen (red circle alert icon, error title, `JetBrains Mono`-style monospace message box, optional scrollable stack pre-formatted, gray hint text). Idempotent: `if (typeof window.__ForzeErrorBoundary__ === 'undefined')` guards all redefinition.
- **Global error listeners in the shim.** `window.addEventListener('error', ...)` catches uncaught script errors — Babel parse errors, undefined references in event handlers, anything outside React's tree. `window.addEventListener('unhandledrejection', ...)` catches async throws (failed fetches, promise chains without `.catch`). Both forward to parent via the same `postErrorToParent({ kind, message, stack, filename, lineno, colno })` helper. The helper is a no-op when `window.parent === window` (standalone visits to `/v/[id]` or `/sites/[subdomain]`), so the dashboard banner is the only consumer; standalone pages still get the React error boundary's UI.
- **Render-call wrap (3 iframe builders).** `app/v/[id]/page.tsx`, `app/sites/[subdomain]/page.tsx`, and `app/dashboard/venture/[id]/[module]/page.tsx` buildPreviewHtml/buildHtmlDocument all changed from `ReactDOM.createRoot(...).render(React.createElement(App))` to: pick up `window.__ForzeErrorBoundary__`, wrap the App in it if available, else render bare. The `typeof window.__ForzeErrorBoundary__ === 'function'` check means the boundary is opportunistic — if the shim ever fails to load (CDN outage, CSP block) the render still goes through with the boundary skipped.
- **Dashboard error banner (`LandingPreviewPanel` in `app/dashboard/venture/[id]/[module]/page.tsx`).** New `LandingPreviewError` type, four new state hooks: `previewError`, `showErrorDetails`, `fixPromptCopied`, plus the existing `iframeLoading` + `iframeKey`. New `useEffect` that listens for `message` events, filters to `data.type === 'forze:landing-error'` AND `event.source === iframeRef.current.contentWindow` (so messages from other iframes / browser extensions don't pollute), and populates `previewError` with all the carried fields. New `useEffect` (extending the existing srcdoc-write effect) that clears `previewError` / `showErrorDetails` / `fixPromptCopied` on every iframe (re)render — fresh render is a fresh chance. Banner UI: absolute-positioned crimson gradient card at the top of the preview area, z-index 25 (above loading spinner), shows error kind tag (React render / Script error / Async error / Runtime), monospace error message in a darkened inset, optional `filename:lineno:colno` line, expandable stack pre, three action buttons (Copy fix prompt / Retry render / Show stack). "Copy fix prompt" calls `buildFixPrompt(err)` which crafts a one-paragraph instruction tailored to the error — heuristic-matches `lucide` / `Shield|Chevron|Arrow|...` → suggests inline SVG, `motion` → suggests Tailwind transitions, `next/` → suggests `<img>` / `<a>`, `process.env` → suggests inlining, fallback → generic "identify undefined reference" — appended with "Output the COMPLETE updated fullComponent." Writes to clipboard via `navigator.clipboard.writeText` with `window.prompt(...)` fallback for sandboxed contexts where clipboard is blocked. The "Retry render" button bumps `iframeKey` to force a fresh `srcdoc` write. The "Show stack" button toggles the expandable pre. Bottom status bar's green-dot "Live Preview" indicator flips to red-dot "Preview error" while `previewError` is non-null.
- Why all four layers together: the validator catches the *static* drift before persistence — anything Babel would silently strip or any pattern the LLM emitted that doesn't compile. The error boundary catches the *render-time* drift — Tailwind classes that need DOM access, hooks that throw, conditional renders that crash. The script/promise listeners catch the *escape hatches* — Babel parse errors that never reach React, async failures the boundary can't see. The banner is how all of that becomes actionable for the founder in 1-2 clicks instead of "open devtools, hunt error, write fix prompt yourself."
- Verification: `npx tsc --noEmit -p .` exit 0. All four layers backward-compatible: the validator is purely additive (existing landings keep their stored fullComponent until next regenerate, which is when the validator runs); the boundary + listeners are gated on `typeof window.__ForzeErrorBoundary__ === 'function'` so the render path is identical if the shim ever fails to load; the banner is gated on `previewError !== null` so dashboards with healthy iframes look unchanged. No existing fields changed, no existing routes touched, no migration needed.

### Previous Session (May 21 2026, evening) — Landing Page Runtime Safety Shim
- **Reported bug:** founder ran the Landing module's edit flow ("add a security shield section") and the rendered iframe crashed with `Uncaught ReferenceError: LucideIcons is not defined` and `Uncaught TypeError: Cannot read properties of undefined (reading 'Shield')`. Root cause: Gemini hallucinates that `lucide-react` is available (a near-universal pattern in modern React landing pages on the open web), but our preview runtime only loads React UMD + Tailwind CDN + Babel-standalone — no icon libraries, no framer-motion, no Next.js components. Same drift happens on initial generation too; it's just more visible in edit mode where the LLM has to invent new UI from a short instruction.
- **Defense-in-depth fix.** Two layers so existing broken pages in the DB also stop crashing, and future generations stop drifting in the first place.
- **Runtime safety shim (`lib/landing-page.ts` → new export `LANDING_RUNTIME_SHIM`).** A `<script>` block injected into every landing iframe AFTER React UMD but BEFORE the Babel `<script type="text/babel">` block. Defines a `GenericIcon` React component that renders a 24×24 inline SVG (checkmark in a circle, `currentColor` stroke, accepts `className`/`style`/`size` props). Exposes 18 common icon-library namespaces (`LucideIcons`, `Lucide`, `LucideReact`, `Heroicons`, `HiIcons`, `FontAwesome`, `FaIcons`, `FontAwesomeIcon`, `RemixIcons`, `BoxIcons`, `BiIcons`, `Feather`, `FeatherIcons`, `FiIcons`, `Tabler`, `TablerIcons`, `TbIcons`, `PhosphorIcons`, `PhIcons`, `MaterialIcons`, `MdIcons`, `IoIcons`, `Ionicons`, `GiIcons`, `GameIcons`, `SiIcons`, `SimpleIcons`) as Proxy objects that return `GenericIcon` for any property access — so `LucideIcons.Shield`, `LucideIcons.WhateverNewIconName`, `Heroicons.User`, etc. all render the fallback SVG instead of crashing. Also populates ~200 PascalCase icon names (`Shield`, `ShieldCheck`, `User`, `Users`, `Check`, `CheckCircle`, `X`, `XCircle`, `ArrowRight`, `ChevronDown`, `Star`, `Heart`, `Mail`, `Phone`, `Globe`, `Zap`, `Sparkles`, `Rocket`, `Lock`, `Settings`, `Search`, `Menu`, `Plus`, `Minus`, `Bell`, `Calendar`, `Clock`, `Home`, `MessageCircle`, `Smile`, `ThumbsUp`, `Trash`, `Upload`, `Download`, `Eye`, `Edit`, `Camera`, `Image`, `Video`, `Play`, `Pause`, `Volume`, `Mic`, `Filter`, `Briefcase`, `Building`, `Code`, `Database`, `Layers`, `Server`, `Box`, `Package`, `ShoppingBag`, `ShoppingCart`, `CreditCard`, `DollarSign`, `TrendingUp`, `BarChart`, `PieChart`, `Activity`, `Award`, `Gift`, `Send`, `Share`, `Link`, `ExternalLink`, `Copy`, `Clipboard`, `Save`, `RefreshCw`, `Power`, `LogIn`, `LogOut`, `Wifi`, `Cloud`, `Sun`, `Moon`, `Cpu`, `HardDrive`, `Smartphone`, `Laptop`, `Monitor`, `Headphones`, `Battery`, `MapPin`, `Map`, `Navigation`, `Compass`, `Truck`, `Plane`, `Car`, `Coffee`, `Book`, `Bookmark`, `FileText`, `File`, `Folder`, `Archive`, `Inbox`, `Trophy`, `Medal`, `Crown`, `Flag`, `Flame`, `Lightbulb`, `Grid`, `List`, `LayoutGrid`, `LayoutDashboard`, `MoreHorizontal`, `MoreVertical`, `Loader`, `Loader2`, `Hourglass`, `Timer`, `Hash`, `AtSign`, `Percent`, `Tag`, `Pin`, `Paperclip`, `Repeat`, `Shuffle`, `Square`, `Circle`, `Triangle`, `Diamond`, `Hexagon`, `Info`, `AlertCircle`, `AlertTriangle`, `HelpCircle`, `LifeBuoy`, `Bug`, `Terminal`, `Github`, `Twitter`, `Facebook`, `Instagram`, `Linkedin`, `Youtube`, `Slack`, `Figma`, …) as direct globals so bare references like `<Shield />` (not prefixed by a namespace) also resolve to `GenericIcon`. Shims `motion` (framer-motion) as a Proxy returning a component that renders the underlying tag with animation-only props (`animate`, `initial`, `exit`, `transition`, `whileHover`, `whileTap`, `whileFocus`, `whileInView`, `viewport`, `layout`, `layoutId`, `drag`, `variants`, etc.) stripped so they don't leak to the DOM as invalid attributes; shims `AnimatePresence` and `LazyMotion` as pass-through children renderers. Shims `NextLink` to a plain `<a>` (forwards `href`, defaults to `#`), `NextImage` to a plain `<img loading="lazy">`. Shims `clsx`, `cn`, `classNames`, `twMerge` to a simple class joiner that handles strings, numbers, arrays, and `{className: bool}` objects. Every shim is gated on `typeof window.X === 'undefined'` so if a future change actually loads the real library it takes precedence.
- **Injected into all 3 iframe HTML builders.** `app/v/[id]/page.tsx` `buildHtmlDocument`, `app/sites/[subdomain]/page.tsx` `buildHtmlDocument`, and `app/dashboard/venture/[id]/[module]/page.tsx` `buildPreviewHtml` all now template `${LANDING_RUNTIME_SHIM}` immediately after the `<script src="https://unpkg.com/@babel/standalone/babel.min.js">` line. The shim runs synchronously when parsed (no defer/async) and React is already loaded by the UMD script above it. Also expanded the destructure inside each `<script type="text/babel">` block from `const { useState, useEffect, useRef } = React;` to include `useCallback, useMemo, useReducer, useLayoutEffect, useId, useTransition, useDeferredValue, Fragment, Children, cloneElement, createContext, useContext, forwardRef, memo` so the LLM can use any common React API without referring to it as `React.useCallback`.
- **Agent prompt tightening (`agents/pipeline.ts`).** Both `SYSTEM_PROMPT` (initial generation) and `EDIT_SYSTEM_PROMPT` (edit mode) now have a "Runtime Constraints (CRITICAL — non-negotiable)" block that enumerates: (a) ALLOWED globals — React + the destructured hooks + Tailwind + `window.__VENTURE_ID__` + native browser APIs; (b) FORBIDDEN — every Lucide variant, Heroicons, FontAwesome, react-icons, Feather/Tabler/Phosphor/Material, bare icon component names like `<Shield />`, framer-motion (`motion.div`, `<AnimatePresence>`), Next.js components (`<Image>`, `<Link>`, `useRouter`), utility libraries (`clsx`, `cn`, `classNames`, `twMerge`), and `import` statements; (c) REQUIRED — for icons, exactly one of: an emoji as a string in JSX, an inline `<svg>` with `viewBox` and SVG children, or a Tailwind-styled `<div>` with a Unicode glyph (with concrete examples). The edit-mode block additionally instructs the LLM that if the existing component contains forbidden references it MUST emit a fullComponent that REPLACES them with safe alternatives — not preserve the broken code. Also patched the features-section spec from `"Icon: a descriptive emoji or icon name"` to `"Icon: an emoji ONLY (e.g. "⚡", "🚀", "🛡️", "💎") — do NOT return a Lucide/Heroicons name like "Shield" or "User"; the rendered component must use the emoji as-is or wrap it in an SVG, never reference an icon library"` because the old wording was ambiguous enough that Gemini could read "icon name" as "Lucide icon name".
- **Why both layers.** The prompt change stops new generations from drifting toward icon libraries. The runtime shim catches (i) any edge-case drift that slips through the prompt, (ii) every landing page already persisted in the DB with `<LucideIcons.X />` references baked in. Without the shim, existing broken pages would stay broken until each founder regenerated. With the shim, they all start rendering immediately — `GenericIcon` shows a checkmark-in-circle SVG wherever Lucide was expected. Visual quality on legacy pages drops slightly, but they stop crashing and the lead-capture form keeps working.
- Verification: `npx tsc --noEmit -p .` exit 0. No regressions to existing landing-page behaviour — the shim only adds new globals, never overrides anything, and all touchpoints (resolveLandingComponent, stripGeneratedCodeFences, isRenderableLandingComponent, buildFallbackLandingComponent) are unchanged. The fallback component in `buildFallbackLandingComponent` is also unaffected because it uses inline emojis and SVGs already — no library references.

### Previous Session (May 21 2026, late) — Inspiration Accuracy Upgrade (Tier 0 + Ground Truth + Image-Grounded Pipeline)
- Diagnosed the accuracy ceiling of the previous build: it was set by *what we feed the model*, not the prompts. og:image is curated marketing art (often a logo on flat color, sometimes a different aesthetic from the actual page); Gemini was being asked to eyeball hex codes from JPEGs (which it's unreliable at); typography defaulted to "Inter" regardless of real site fonts; the React landing-page generator never saw the inspiration at all. Net 5-6/10 accuracy. This session targets ~8/10 by replacing inference with measurement at the three places the pipeline was hallucinating.
- **Tier 0 capture via Microlink.** New module `lib/inspiration/remote-capture.ts` calls the Microlink API (free tier, no auth; `MICROLINK_API_KEY` env attaches paid tier) with `screenshot=true&palette=true&meta=true&viewport.width=1440&viewport.height=900&waitUntil=networkidle0&screenshot.type=jpeg&screenshot.quality=85`. Downloads the resulting screenshot via the existing fetchWithTimeout (SSRF re-validated on the response URL). Parses the Vibrant palette in both possible Microlink shapes — `palette` as array of hex strings AND as `{ Vibrant: { hex, population }, DarkVibrant: …}` object — sorts by population, dedupes, returns up to 6 most-dominant. Also surfaces `data.color` (brand), `data.backgroundColor` (page bg), `data.title`, `data.publisher`. `lib/inspiration/screenshot.ts` `CaptureResult` got a new `tier: 0` and a `groundTruth` field carrying palette/brandColor/backgroundColor/title/description/publisher. New tier slots in front of og:image with a try/catch — any Microlink failure (rate limit, timeout, malformed response) silently falls through to the existing four-tier ladder so this can never break the analyze flow. Gated by `INSPIRATION_USE_REMOTE_SCREENSHOT=false` if cost ever needs to drop.
- **HTML/CSS scraper as ground truth.** New module `lib/inspiration/html-scrape.ts` fetches the live HTML (capped at 800KB, 8s timeout) and runs dependency-free regex extraction across five paths: Google Fonts hrefs (`?family=Inter:wght@…` → "Inter"), Typekit/Adobe Fonts presence (flagged but family-unknown because we don't fetch external CSS), inline `<style>` `@font-face` declarations, `<link rel="preload" as="font" href="…woff2">` filename parsing (matches `Sohne-Regular.woff2` → "Sohne"), and direct `font-family:` declarations in inline styles. Also extracts every `--*` CSS custom property from inline `<style>` blocks (skipping `var(...)` references to avoid cascade noise), inline hex codes from `style=""` attributes, the `<title>`, the meta description, and `<html lang>`. Filters out system-stack fallbacks (Inter, system-ui, sans-serif, Helvetica, etc.) so the result is only the real custom families. Returns ScrapedTokens with assigned `heading`/`body`/`mono` slots (mono = first family matching /mono|code|jetbrains/i, heading = first non-mono, body = second non-mono or same as heading).
- **Ground-truth clamping in vision.** `lib/inspiration/vision.ts` `analyzeImageWithGemini` now builds a `buildGroundTruthBlock(capture)` block appended to the user message. The block lists Vibrant brand color (mandate confidence ≥95), page background, dominant palette (instructs Gemini to pick secondary/accent FROM this list), real detected font families (mandate exact name in `typography.headingFamily` / `bodyFamily`), real CSS variables filtered to the design-relevant ones (`/color|bg|background|brand|primary|accent|secondary|text|surface|radius|space|spacing|padding|gap|font/i`, top 14 by listing order), and inline hex evidence. Then post-parse, **trust-but-verify** clamps fire deterministically: `gt.fontHeading` rewrites `typography.headingFamily` to `"Family Name", existing-fallback` (Gemini hallucinating "Inter" gets overridden by "Söhne" detected via @font-face); CSS `--primary`/`--accent` hex variables override `colors.primary`/`colors.accent` at confidence 97/95; CSS `--radius` overrides button + card radius at confidence 95; Vibrant brand color overrides `colors.primary` at confidence 98; Vibrant background color overrides `colors.background`; remaining secondary/accent slots get filled from the palette if Gemini returned the same hex as primary (a common failure mode). Net effect: any field we can actually measure on the live page is locked to the measured value — the model can derive neutrals/text/surface/spacing scale but cannot drift from real measurements.
- **Image-grounded landing pipeline.** Three changes wire the screenshot into the React generator's Gemini call: (1) `lib/gemini.ts` `streamPrompt` and `streamGeminiPrompt` accept an optional `inlineImages: InlineImagePart[]` parameter — when populated, the user message becomes a multi-part array `[{ inlineData }, …, { text }]` instead of a string; Grok path drops images and degrades to text-only. (2) `lib/queries/inspiration-queries.ts` `setVentureInspirationTokens` accepts an optional `referenceImages: InspirationReferenceImage[]` parameter that writes them to `venture.context.inspirationReferenceImages` alongside the tokens (clean separation — the DesignTokens schema stays pure). The apply route downloads up to 2 of the analysis's captured screenshots via `capture_metadata.perUrl[].sourceUrl`, caps each at 600KB, base64-encodes, persists. Unapply nulls them too. (3) `agents/pipeline.ts` reads `venture.context.inspirationReferenceImages`, builds the `inspirationImages` array, and passes it to `streamPrompt` in BOTH initial generation and edit mode. The prompt text changes too: when images are attached, the inspiration block opens with "**A screenshot of the inspiration page is attached to this message.** Look at it. Your generated landing page must visually echo what you see…" and closes with "**The attached screenshot is the visual target.** When the briefing and the screenshot conflict, the screenshot wins." The text briefing is now your written instructions, the screenshot is the reference photo.
- **Self-consistency on vision.** New `analyzeImageWithSelfConsistency(capture, ctx)` wrapper in vision.ts runs the canonical extraction twice in parallel and reconciles: mood = mode (tie → higher `confidenceByCategory.overall`), confidence = median per dimension, brand personality = longer of the two strings (Gemini sometimes truncates on first pass), `designSignals.notableInteractions` = deduped union (capped at 8), `typography.sizes.h1.mobile`/`h2.mobile` = first non-empty across runs. Color slots are NOT touched — they're already clamped to ground truth so both runs have identical values for them. Cost: 2× vision calls per URL (still parallel — no latency hit), gated by `INSPIRATION_VISION_SELF_CONSISTENCY=false` opt-out. The analyze route now calls this wrapper instead of `analyzeImageWithGemini` directly. Net effect: mood + qualitative output stabilises run-to-run; one-off "this site is luxury-premium" hallucinations vanish.
- Verification: `npx tsc --noEmit -p .` exit 0, `npm run build` exit 0. Shipped as 5 commits — Microlink Tier 0 + groundTruth field, vision ground-truth prompt + post-parse clamps, HTML/CSS scrape + clamp-from-css-vars + analyze-route fold-in, image-into-pipeline (gemini.ts + queries + apply + pipeline.ts together since they're an atomic capability), self-consistency wrapper + analyze swap. All routes backward-compatible: missing Microlink data falls through to og:image, missing scrape data leaves the vision-derived value, missing reference images runs text-only briefing.
- Expected accuracy: a founder pasting Stripe now gets `#635bff` as primary (Vibrant-measured, clamped), real font name from `<link rel=preload as=font>` parsing or Google Fonts href, real `--radius` from `:root` CSS variables, and a generated landing page where the React generator literally saw the Stripe hero screenshot during generation. Same for Linear (dark mode, accent, specific Geist font), Vercel (gradient mesh, exact brand blue), or anything with conventional CSS architecture. Sites that obfuscate everything behind hashed CSS still work — they just degrade to the previous Gemini-eyeball flow.

### Previous Session (May 21 2026, mid) — Inspiration System Enhancements (docs/INSPIRATION_SYSTEM_IMPROVEMENTS.md)
- Executed the improvement plan against the working "Generate from Inspiration" feature. All ten plan areas were addressed across one Phase-1 + Phase-2 sweep, kept fully backward-compatible (every new column is nullable / additive, every new tab is conditional, the analyze + patch + apply routes still respond to single-URL no-weight callers exactly as before).
- Migration `028_inspiration_enhancements.sql` extends `inspiration_analyses` with: per-pass evidence (`pass1_design_system`, `pass2_components`, `pass3_antipatterns`, `detected_sections`, `interaction_states`, `context_relevance`), accessibility audit (`accessibility_report`, `has_contrast_issues`, `requires_manual_review`), quality (`quality_score`, `recommendations TEXT[] NOT NULL DEFAULT '{}'`), merge weighting (`merge_weights`), and cost/lifecycle (`extracted_at`, `gemini_cost_tokens`). Added partial index `idx_inspiration_analyses_needs_review` so the dashboard can paint a "needs review" badge cheaply. Migration is `IF NOT EXISTS` on every column — re-runs are safe.
- `lib/inspiration/accessibility.ts` — pure WCAG 2.1 validator over a `DesignTokens` object. Implements sRGB→linear conversion, relative luminance, contrast ratio. Checks body text on background + on surface, secondary text on background, primary button text on primary fill (auto-picks white vs black via `preferredOn`), accent on background, base font size (14/16px thresholds), normal line-height (1.35/1.5 thresholds), and focus indicator vs input border distinction. Returns severity-weighted score (criticals cost 15, warnings 5) plus `hasContrastIssues` / `requiresManualReview` flags. Zero I/O — pure function. Runs in the analyze and PATCH routes immediately after token extraction / edit.
- `lib/inspiration/scoring.ts` — pure quality scoring across five dimensions: colorConsistency (monotonicity + spread of the neutral palette via HSL lightness), readability (re-uses accessibility findings narrowed to contrast + typography), responsiveness (presence of mobile H1/H2 + breakpoint ordering), componentCohesion (button/card/input radius spread), trustSignals (mood-keyed score with personality-length bonus). Emits 0–100 per dimension + overall + flat `recommendations` string array capped at 12 entries for UI legibility.
- `lib/inspiration/passes.ts` — three enrichment Gemini Vision passes, each best-effort (failures return null and persist null without blocking the primary token extraction). `detectSections` labels visible hero/features/social_proof/pricing/faq/cta/footer/logos with confidence + bbox + dominant colors + layout pattern. `analyzeComponentPatterns` extracts default + hover + focus + active + disabled states for buttons / cards / inputs / links with per-state bg/text/border/shadow/transform descriptors. `analyzeAntiPatterns` returns `doNotCopy` / `accessibilityWins` / `intentSignals` / `responsiveRules` / `contextRelevance` (trust signals, differentiator, CTA strategy, responsive pattern). All passes share a `PassContext` (ventureName / oneLiner / audience / ventureType) that biases the model toward signals relevant to the specific venture instead of generic extraction.
- `lib/inspiration/vision.ts` extended with `buildContextPreamble(ctx)` so the same `PassContext` flows into the canonical token extraction call — Gemini gets the venture name + one-liner + audience + category appended to the system prompt and is told to bias the extraction toward trust signals / CTA strategy / feature hierarchy that fit the audience. Backward-compatible: passing no ctx works exactly as before.
- `lib/inspiration/tokens.ts` `mergeTokens(analyses, weights?)` — colors now use weighted confidence (`confidence × weight` decides each slot), section padding uses weighted average instead of median, brand mood uses weighted vote (sum of weights per mood), personality strings are ordered by weight. Equal-share fallback (`1 / N`) means existing single-call sites with no weights produce identical output. Heading-family selection now prefers serif only if a meaningful-weight (≥0.2) analysis is serif-leaning.
- `lib/inspiration/refine.ts` + `app/api/ventures/[id]/inspiration/[analysisId]/refine/route.ts` — post-apply refinement. Founder describes an issue ("primary button is hard to see on features section"), optionally tags the affected component (button / card / text / spacing / hero / overall), the route revalidates accessibility on current tokens, sends a tight system prompt to Gemini that's explicitly capped at 5 path changes and forbidden from touching `brand.mood` / `designSignals.*`, and returns `{ adjustments, rationale[], summary }` as a preview the user must accept. Accept replays the adjustments through the existing PATCH route so they go through the same Zod validation + persistence as a manual edit.
- API route changes: `POST /analyze` now runs vision per URL + three enrichment passes on the primary capture in a single `Promise.all`, builds `PassContext` from `venture.context.research.{oneLiner|summary|description, audience|targetAudience, category|industry}`, then runs deterministic accessibility + scoring over the merged tokens, and persists everything (accessibility / score / recommendations / sections / components / antipatterns / mergeWeights / extractedAt) in one update. Response payload extended with `accessibility / qualityScore / recommendations / detectedSections / componentPatterns / antiPatterns` so the studio gets immediate values without a follow-up GET. `PATCH /[analysisId]` accepts optional `mergeWeights` and re-merges from `raw_vision` when present (so the slider live-re-blends colors / mood / padding), and recomputes accessibility + scoring on every token edit so the Quality tab never shows stale findings. `GET /[analysisId]` surfaces all the new fields under camelCase keys.
- `lib/schemas/inspiration.ts` adds `MergeWeightsSchema` (hostname → 0..1, capped at 5 keys) and `InspirationRefineInputSchema` (issue 8-800 chars + optional affectedComponent enum + optional suggestion); both `InspirationAnalyzeInputSchema` and `InspirationTokenPatchSchema` now carry an optional `mergeWeights` field. `lib/queries/inspiration-queries.ts` `InspirationAnalysisRow` + `updateInspirationAnalysis` patch type extended with the migration-028 fields so the route helpers stay strongly typed end-to-end.
- UI: `components/venture/InspirationStudio.tsx` extended with three new tabs — Quality (overall score + per-dimension bars + severity-coded accessibility findings with critical-issue badge on the tab itself + recommendations list), Sources (per-URL weight sliders, only rendered when multi-URL — slider drag triggers a debounced PATCH that re-merges from raw_vision and updates tokens + accessibility + score live), and Refine (component dropdown + issue textarea + suggested-patch preview with per-path rationale that can be accepted to flow through normal PATCH persistence or discarded). New client-side types mirror the server's `AccessibilityFinding` / `QualityScoreClient` / `RefineSuggestionClient` shapes. Existing five tabs (Colors / Typography / Spacing / Components / Preview) untouched and continue working byte-identical.
- Verification: `npx tsc --noEmit -p .` exit 0. `npm run build` exit 0 — full Next.js 16 build clean, all new + existing inspiration routes registered (including new `/refine` route under `[analysisId]`). Shipped as 12 surgical commits each on a single concern (migration, accessibility module, scoring module, passes module, vision context preamble, weighted merge, query helper extension, schema additions, analyze route wiring, GET/PATCH extension, refine route + lib, studio UI tabs). Migration 028 must be applied to Supabase before the enrichment columns can persist in production; routes gracefully degrade if columns are missing (the SELECT only references columns through interface fields that default to undefined).

### Previous Session (May 20 2026) — Generate from Inspiration MVP
- Built the end-to-end "Generate from Inspiration" feature per `docs/GENERATE_FROM_INSPIRATION.md`. A founder pastes 1–3 inspiration URLs (e.g. stripe.com, vercel.com) into a new sidebar entry under BUILD, Forze captures a representative image of each, runs it through Gemini Vision, distills a deterministic DesignTokens object, lets the founder refine the tokens in an interactive editor with confidence badges + per-token locks, then applies them to `ventures.context.inspirationTokens` so the next landing-page generation uses them as a hard design override.
- Adapted spec to current stack: the doc assumed a Playwright/Chromium pipeline for tiers 1–2 (300MB+ runtime, long cold starts on Vercel, doesn't pair with `--experimental-https` dev). Collapsed to four fetch-based tiers — og:image / twitter:image (designers curate this and it covers ~85% of real inspiration URLs), apple-touch-icon (decent res), favicon (last resort), user-uploaded image (final escape). All tiers go through a strict SSRF guard (`isSafeRemoteUrl`) that blocks localhost, RFC1918, link-local, and metadata IPs.
- Migration `027_inspiration_analyses.sql` adds two tables — `inspiration_analyses` (per-row audit of urls→tokens→applied_at) and `inspiration_rate_limits` (per-user/per-venture/per-day counter with unique window key) — both with RLS gated on `auth.uid() = user_id` and an `updated_at` trigger. The actual hand-off to the pipeline agent rides on `ventures.context.inspirationTokens` (existing JSONB column) so no ventures-schema change was needed.
- New libs: `lib/schemas/inspiration.ts` (Zod DesignTokensSchema with color/typography/spacing/component sub-trees + confidence scores + sources for multi-URL blends; InspirationUrlSchema with SSRF guard; PatchSchema for editor PATCH calls), `lib/inspiration/screenshot.ts` (four-tier capture cascade), `lib/inspiration/vision.ts` (Gemini-3-flash multimodal with one-shot prompt that returns the full tokens shape; on Gemini failure returns defaulted tokens with confidence=25 so the editor still loads), `lib/inspiration/tokens.ts` (defaultTokens / setAtPath / applyAdjustments / mergeTokens for multi-URL blending using highest-confidence-wins on colors, median on numeric spacing, mode on brand mood; tokensToPromptDigest + tokensToCssVarBlock for the pipeline agent's prompt). `lib/queries/inspiration-queries.ts` adds analysis CRUD, rate-limit upsert helpers, and `setVentureInspirationTokens` (RLS-first with admin-client fallback for member-but-not-owner writes).
- Four API routes added under `/api/ventures/[id]/inspiration/*`: `POST /analyze` (synchronous validate → capture → vision → merge, rate-limit gated, returns tokens immediately so the editor opens instantly without polling); `GET /` (history list with rate-limit view); `GET/PATCH/DELETE /[analysisId]` (load + per-path token edit + delete); `POST/DELETE /[analysisId]/apply` (push tokens onto venture.context.inspirationTokens / clear them). Editors and above can mutate, viewers can only read. The PATCH endpoint deep-merges `adjustments` into the saved tokens object, respects `lockedPaths`, and re-validates through Zod before persisting.
- Pipeline agent integration: `agents/pipeline.ts` `runPipelineAgent` now detects `venture.context.inspirationTokens`, parses + validates with `DesignTokensSchema`, and injects a "Inspiration Design Tokens (HARD overrides — apply EXACTLY)" block into the user message alongside the existing Research/Branding/Marketing blocks. The block contains both a human-readable digest and a CSS-var declaration block the LLM is instructed to emit at the top of its inline `<style>` tag so future token edits cascade through `var(--insp-*)` lookups. Edit-mode (existing landing) is intentionally untouched — inspiration tokens only apply on full regeneration.
- UI: new server page `app/dashboard/venture/[id]/inspiration/page.tsx` wrapping client `components/venture/InspirationStudio.tsx`. Studio has three states (empty URL inputs, analyzing, result), 5-tab editor (Colors with hex swatches + neutral scale + lock toggles + confidence badges, Typography with live preview, Spacing, Components with rendered button/card preview, full Preview with hero + feature grid), undo via "Load" from history, download tokens as JSON, Apply / Remove buttons that wire to the apply route, and a "Open Landing Module →" shortcut. PATCH calls debounce 600ms so the editor feels instant while still persisting every change. New sidebar entry under BUILD section (accent `#9C7B5A`, ✦ glyph) sits next to Testimonials; `moduleHref` + `isModuleActive` extended to route `/dashboard/venture/[id]/inspiration`.
- Verification: `npx tsc --noEmit -p .` exit 0, no errors. `npm run build` exit 0, full Next 16 build clean — all four new inspiration routes registered, the dashboard inspiration page registered, no regressions to the ~100 existing routes. Migration `027` must be applied to Supabase before the analyses can persist in production; the pipeline integration is gated on a Zod parse so an absent or malformed `inspirationTokens` key falls back to the existing branding-only flow with zero behaviour change.
- Shipped as 14 surgical commits per repo rule (one feature per commit): migration, schemas, token-merge lib, screenshot lib, vision lib, queries lib, four API routes, pipeline integration, server page, client component, sidebar wiring.

### Previous Session (May 19 2026) — Full Build Audit (read-only)
- Ran `docs/COMPLETE_BUILD_AUDIT_PROMPT.md` end-to-end as a verification pass; no source code modified. Audit result: PASS. Platform continues to run as it is.
- Build verification: `npx tsc --noEmit -p .` → exit 0, no errors. `npm run build` → exit 0, full Next.js 15 route table compiled cleanly (~100 dynamic + ~12 static routes, Proxy middleware emitted). The historical "agents/pipeline.ts:287 parse error" blocker referenced in earlier sessions and in the audit doc is no longer active — line 287 sits inside the landing-page system-prompt template literal and parses fine; both tsc and next build confirm.
- Structure verification: `agents/` holds all expected modules (genesis, identity, content, pipeline, feasibility, shadow, plus orchestrator/cohort-comparator/general/investor-kit/launch-autopilot/mvp-scalpel/variant-generator). `lib/` has db, auth, queries, gemini, plus feature-scoped helpers. `lib/queries/` has blog/campaign/routine query splits. `components/` organized by feature (venture, ui, blog, landing, marketing, billing). `db/migrations/` sequential through `026_routines_per_user_claim.sql` (one historical 010_* duplicate filename for instagram vs investor-kit, harmless and pre-existing).
- Security spot-checks: no live API keys hardcoded in `.ts`/`.tsx` (grep for `AIza…` / `sk-…` patterns → zero matches outside `.agents/skills` docs). `GEMINI_API_KEY` is read only on the server in `lib/gemini.ts`, `lib/email-generator.ts`, `lib/marketing-image-gen.ts` — never client-prefixed. All `NEXT_PUBLIC_*` keys are legitimately public (Supabase anon, Razorpay publishable). `.env.local` confirmed present and covered by `.gitignore` (`.env*.local` + `.env`).
- API hygiene spot-checks: `requireAuth(` appears across 43 route files under `app/api/`; Zod schemas (`z.object`/`z.string`/`z.number`) appear in 27 route files. Public submission endpoints (`/api/ventures/[id]/leads`, `/api/ventures/[id]/feedback`, `/api/ventures/[id]/track`, blog views, investor/invite lookups, OAuth callbacks) are intentionally unauthenticated and gated by UUID validation + venture-scope checks. CRON endpoint uses the `x-vercel-cron` fallback wired in the prior session so it works without `CRON_SECRET` being set.
- Part 9 verification commands all run (read-only): `npm run lint` exits 0 but is misconfigured — the package.json script is `next lint` and the shell receives the literal arg `lint` as a directory, so no lint actually executes (pre-existing repo quirk, not a regression). `npm audit` reports 11 vulnerabilities (4 moderate, 6 high, 1 critical) all in transitive deps of `next` / `postcss` / `orchid→ws` / `tea→orchid`; fix available via `npm audit fix` but deferred because it would bump framework versions outside this read-only pass. `npx depcheck` flags `codex` + `html-to-image` as unused runtime deps and `autoprefixer` + `postcss` as unused devDeps (the latter two are false positives — both are required by the Tailwind/PostCSS build pipeline). One source file exceeds 100KB: `app/dashboard/venture/[id]/[module]/page.tsx` at 178KB (legitimate — this is the central venture module workspace; pre-existing, not a regression). `console.*` calls: 16 across 10 app files plus one in `agents/shadow.ts`, all intentional error/warn logs.
- `CRON_SECRET` is intentionally absent from `.env.local` and replaced by the `x-vercel-cron` header path documented in the May 19 routines session.
- Net: every blocker the audit prompt calls out (Part 10) is resolved; Part 12 checklist passes (build 0, tsc 0, no secrets, PROGRESS.md updated, ready for prod). No follow-up changes required for the platform to keep running.

### Previous Session (May 19 2026)
- Fixed broken landing-page URLs producing `*.www.forze.in` (Chrome error `chrome-error://chromewebdata/` / "Unsafe attempt to load URL"). Root cause: `NEXT_PUBLIC_APP_URL` is `https://www.forze.in`, and three call sites built tenant URLs as `${subdomain}.${url.host}` — yielding `meritledger-v1.www.forze.in`, which DNS does not serve (the Vercel wildcard is `*.forze.in`). Same env value also broke the proxy's tenant detection: `getBaseHost()` returned `www.forze.in`, so `feedflow.forze.in` failed the `.endsWith('.www.forze.in')` check and was never rewritten to `/sites/[subdomain]`.
- Chose the code-only fix over flipping the env var to preserve all other consumers untouched: Gmail OAuth `redirect_uri` (must match Google Cloud Console allow-list), Supabase email confirm + password-reset links (must match Supabase allow-list), investor-kit / campaign / invite link bases, and `lib/routine-executor.ts` internal cron callbacks all keep pointing at `www.forze.in`. Instagram OAuth was already safe because `app/api/integrations/[provider]/{connect,callback}/route.ts` uses `request.nextUrl.origin` instead of the env.
- Three surgical files, four edits: `proxy.ts:getBaseHost` strips a leading `www.` from the parsed host so tenant detection works against the apex. `app/dashboard/venture/[id]/[module]/page.tsx` strips `www.` in both the header URL pill (line ~1139) and `buildVentureSiteUrl` (the "open published site" anchor + `window.open` target). `agents/pipeline.ts:deployLandingPage` strips `www.` before composing the absolute subdomain URL it returns to the pipeline output. Display recomputes from `subdomain + host` on every render, and the pipeline doesn't persist its returned URL anywhere (grepped `deployedUrl|deployed_url|landingUrl|landing_url|siteUrl|site_url|publishedUrl|published_url` — zero matches), so old ventures auto-heal without a backfill.
- Verified prerequisite with user: `*.forze.in` is the configured Vercel wildcard domain, so DNS + cert for `meritledger-v1.forze.in` already resolve once the URL is generated correctly.

### Previous Session (May 19 2026)
- Fixed "routines only fire when I press the admin button." Root cause: Vercel Cron was scheduled hourly (`0 * * * *`) AND `/api/cron/run-routines` 401'd whenever `CRON_SECRET` wasn't set in the Vercel project env, so even when the schedule ticked the request was rejected — and the hourly cadence itself was too slow to match the user's expectation of "fires at the time I set."
- Tightened scheduler precision: `vercel.json` cron for `/api/cron/run-routines` is now `* * * * *` (every minute), so routines fire within ~60 seconds of `next_run_at`. Updated the panel's "When do routines fire?" notice from the old "hourly tick at HH:30 IST" lag table to a one-line "ticks every minute" reassurance.
- Hardened cron auth in `app/api/cron/run-routines/route.ts` so it also accepts the `x-vercel-cron` header. Vercel sets that header internally on every cron invocation and strips it from inbound external requests at the edge — safe enough that the cron now Just Works on first deploy without the user having to configure `CRON_SECRET`. Existing `Bearer <CRON_SECRET>` / `Bearer <ROUTINES_CRON_SECRET>` / `x-routines-cron-secret` paths preserved for curl + tests.
- Added a defensive second firing path so the experience never depends on platform cron alone: migration `026_routines_per_user_claim.sql` adds `claim_due_routines_for_user(uuid, int)` (same `FOR UPDATE SKIP LOCKED` CTE + `UPDATE…RETURNING` semantics as `claim_due_routines`, but the candidate set is filtered to a single `user_id`). New helper `claimDueRoutinesForUser` in `lib/queries/routine-queries.ts` wraps the RPC. New auth'd endpoint `POST /api/routines/fire-mine` (Node runtime, `maxDuration=300`) claims and executes only the calling user's due routines and returns a `{ claimed, succeeded, failed, skipped }` summary.
- Wired `components/venture/RoutinesPanel.tsx` to call `/api/routines/fire-mine` (a) silently on panel mount per `ventureId`, (b) silently after `onCreated` so a routine whose first `next_run_at` is already past fires immediately, and (c) visibly via a new "Run due now" header button next to "New routine." Added a `FireResultBanner` that summarizes the last manual fire and can be dismissed. The on-mount sweep is now belt-and-suspenders behind the every-minute cron — kept for resilience when Vercel cron is paused/throttled.
- Verification: `npx tsc --noEmit -p .` clean. End-to-end confirmed by the user: a routine fired at its scheduled time post-deploy. Migration `026` must be applied to Supabase before the per-user claim endpoint can succeed in production.
- Shipped as six surgical commits (per repo rule on commit granularity): `f88cd7c` migration, `5b9587a` query helper, `b8ebb49` fire-mine route, `5555675` cron auth fallback, `a341178` minute schedule, `ba56a3f` panel wiring.

### Previous Session (May 16 2026)
- Shipped the testimonials & feedback system per `C:\Users\arham\.claude\plans\we-have-to-now-quizzical-tome.md`. Migration `025_testimonials.sql` adds `testimonials` (venture-scoped, FK to ventures, optional `lead_id` FK to `leads`, `kind` discriminator for testimonial/feedback, featured/archived booleans, indexes by venture and lead) and `platform_feedback` (user-scoped, category check constraint, page_url + user_id/email).
- Extended `lib/queries.ts` with `createTestimonial` (best-effort lead-link via case-insensitive email lookup on the venture's leads), `listTestimonials` with filters, `getTestimonialById`, `setTestimonialFeatured`, `setTestimonialArchived`, `deleteTestimonial`, and `createPlatformFeedback`. All inserts go through the existing `withRetry()` helper. New types `Testimonial`, `TestimonialKind`, `TestimonialFilters`, `PlatformFeedback`, `PlatformFeedbackCategory` exported.
- Built the public submission flow: `app/feedback/[ventureId]/page.tsx` (server, public, robots:noindex, pulls venture name + brand accent via `getVenturePublic`), `app/feedback/[ventureId]/FeedbackForm.tsx` (client, segmented testimonial/feedback toggle, inline success state), and `app/api/ventures/[id]/feedback/route.ts` (mirrors the leads route — CORS+OPTIONS, UUID regex on ventureId, name/email/quote validation, length guards 10–2000 on quote).
- Built the dashboard view: `app/dashboard/venture/[id]/testimonials/page.tsx` (server, `requireAuth()` + `getVenture()`, suspense-wrapped client), `components/venture/TestimonialsDashboardClient.tsx` (KPI tiles, segmented All/Testimonials/Feedback/Featured/Archived filter, search, card list with Feature/Archive/Copy-quote actions, "Copy public link" header action). New API endpoints `GET /api/ventures/[id]/testimonials` and `PATCH|DELETE /api/ventures/[id]/testimonials/[testimonialId]` with `getVentureAccess` membership gating (viewers can read via GET but cannot mutate).
- Added a Forze platform feedback channel: `components/ui/PlatformFeedbackButton.tsx` (modal with bug/feature/praise/other categories, auto-captures current `pathname` as `page_url`, escape-to-close) plus `POST /api/platform-feedback` behind `requireAuth()`. Mounted in `app/dashboard/layout.tsx` under the existing "Manage Projects" footer slot.
- Surfaced the Testimonials route in the sidebar by extending `moduleHref` / `isModuleActive` in `app/dashboard/layout.tsx` and appending a new top-level nav button below CRM (accent `#B86A8E`, `❝` glyph). Did NOT modify `components/venture/VentureHeader.tsx` `VENTURE_TABS` because it's typed `ModuleId` and Testimonials is a sibling feature, not an agent module (same pattern as CRM/Campaigns).
- Verification: `npx tsc --noEmit` clean. Migration must still be applied with `psql` / Supabase migration runner. End-to-end testing (public form submit → CRM linkage, dashboard feature/archive, platform feedback) is gated on running the migration.

### Previous Session (May 15 2026)
- Shipped wildcard subdomain routing per `docs/WILDCARD_SUBDOMAINS_PROMPT.md`. Migration `023_venture_subdomains.sql` adds a nullable `subdomain` column with a case-insensitive partial unique index, a `slugify_venture_name` helper, and a one-shot backfill that walks every existing venture and assigns a unique slug (collision-suffixed with `MD5(id || attempt)[:5]`). `lib/queries.ts` now carries `subdomain` on the `Venture` type, generates a unique slug on `createVenture` (collision-suffixed via `Math.random` then timestamp fallback), and exposes `getVentureBySubdomain` + `updateVentureSubdomain` for the rewrite path. New root `middleware.ts` derives the base host from `NEXT_PUBLIC_APP_URL`, strips the subdomain label off `*.host`/`*.localhost`, and `NextResponse.rewrite`s tenant hosts to `/sites/[subdomain]` while letting apex/`www`/unrelated hosts and `/api`/`_next` paths pass through. New `app/sites/[subdomain]/page.tsx` mirrors the legacy `/v/[id]` iframe shell and fetches via the new `app/api/sites/[subdomain]/preview/route.ts` (resolves the venture by subdomain, returns `id` so the iframe's `__VENTURE_ID__` injection keeps lead/track endpoints working). Dashboard preview "open in new tab" button + a new header pill both render `https://<subdomain>.<host>` via a `buildVentureSiteUrl` helper that falls back to `/v/[id]` when the env URL is missing. `agents/pipeline.ts` `deployLandingPage` now resolves the venture's subdomain via `getVenturePublic` and returns the absolute subdomain URL (falls back to `/v/[id]`). `npx tsc --noEmit` clean.

### Previous Session (May 8 2026)
- Implemented backend CRM fixes from `backend-crm-fixes.md`: added migration `022_crm_email_tracking.sql` for `outreach_campaigns.thread_ids` and `outreach_messages`, added outreach message query helpers, switched CRM dispatch from mock logging to authenticated Gmail sends via the user's connected Gmail, stored Gmail message/thread IDs, and added `GET /api/ventures/[id]/crm/replies` to read non-SENT replies from tracked Gmail threads.
- Updated Instagram validation backend behavior: requested the Meta comment-reading scopes from the plan, preserved existing Instagram Business scopes, and updated comment-permission diagnostics to recognize `instagram_manage_comments` and `instagram_business_manage_comments`.
- Reworked `GET /api/ventures/[id]/crm/analytics` so the funnel top metric now uses social reach/impressions from published marketing assets instead of local pageview counts; conversion is now leads divided by social reach while retaining the frontend-compatible `visitors`, `leads`, `conversionRate`, `rawAnalytics`, and `socialBreakdown` response shape.
- Verification: targeted backend CRM TypeScript check passes. Full `npm run build` remains blocked by the pre-existing unrelated parse error in `agents/pipeline.ts` at line 287.

### Previous Session (May 8 2026)
- Shipped the CRM dashboard plan for `/dashboard/venture/[id]/crm`: `CrmDashboardClient.tsx` is now the single CRM UI with Overview, Inbox, Leads, Outreach, and Pipeline tabs using Forze CSS variables and inline styles. Added KPI cards, conversion funnel, source attribution, 7-day SVG sparkline, filtered inbox, email/social lead segments, editable lead status, hard delete, CSV export, outreach preview + confirmation modal, and campaign pipeline totals.
- Added CRM email-lead APIs: `GET /api/ventures/[id]/crm/leads/email`, `PATCH/DELETE /api/ventures/[id]/crm/leads/[leadId]`, and `GET /api/ventures/[id]/crm/leads/export?type=email|social`. Added `getLeadById` and `deleteLead` helpers in `lib/queries.ts`. Hardened dispatch validation and ownership checks so outreach sends only to email leads with status not `lost`.
- Verification: targeted CRM TypeScript check passes. Full `npm run build` is blocked by the pre-existing unrelated parse error in `agents/pipeline.ts` at line 287.

### Previous Session (April 20 2026)
- Shipped the public blog system per `BLOG_SETUP_PROMPT.md`. Migration `017_blog_posts.sql` creates `blog_posts` + `blog_views` with RLS (public reads on `published = true`, authors manage own rows) and an `increment_blog_view_count(p_post_id)` SECURITY DEFINER RPC so anonymous visitors can bump the counter without UPDATE privileges. Added `lib/schemas/blog.ts` (input/row/summary Zod shapes), `lib/queries/blog-queries.ts` (Supabase list/detail/related/view helpers), three API routes under `/api/blog/posts`, `/blog` listing + `/blog/[slug]` detail pages using Next 16 `params: Promise<...>`. Added `BlogCard` + `BlogMeta` (JSON-LD `BlogPosting` + metadata helpers) and lightweight `format-date.ts` (skips adding `date-fns`). Added `.blog-content` typography block in `globals.css` since `@tailwindcss/typography` isn't installed. Seeded 3 launch posts via `db/seeds/blog-posts.sql` (idempotent via `ON CONFLICT (slug)`). `npx tsc --noEmit` clean.

### Previous Session (April 19 2026)
- Added Campaigns as a full sidebar module + venture tab (shared `VentureHeader`, `?tab=` deep link, Suspense boundary for `useSearchParams`). Fixed `/api/ventures/[id]` response-shape bug and cross-venture URL-tamper guard on the detail page. Switched `CampaignDetail` auto-poll from `POST /poll-replies` to `GET` (cheap read path). Hoisted `const sendInput = input.data` in `app/api/campaigns/[id]/send/route.ts` so nested closures carry the narrowed Zod type.
- Fixed RLS violation on `createCampaign` — migration `015_campaign_rls.sql` enables RLS and adds policies for `campaigns`, `campaign_leads`, `campaign_analytics`, `campaign_replies`, `gmail_integrations`, `rate_limit_events` matching the `009_marketing_automation.sql` pattern (direct-owned rows gate on `auth.uid() = <owner>`, child rows gate via `EXISTS` on the parent campaign).

### Previous Session (April 18 2026)
- Built complete Auto-GTM Layer MVP (20 files): DB migration `013_auto_gtm.sql`, Zod schemas, campaign query helpers, Gmail OAuth (`lib/gmail-oauth.ts`), Gmail sender (`lib/gmail-sender.ts`), Gemini email generator (`lib/email-generator.ts`), email utilities, 8 API routes under `/api/campaigns/`, `/api/track/`, `/api/integrations/gmail/`, and 3 UI components + 2 pages for campaign management
- Rewrote `app/dashboard/layout.tsx` sidebar: replaced cramped 272px tree (11.5px text, 26px rows) with two-column design — 52px left rail (project icons + dark mode + user avatar) + 220px right panel (venture picker dropdown + 36px module rows with 13px text). Sidebar collapses to 52px left rail only. Removed ~430 lines of dead code (expandedProjects/Ventures state, toggleProject/toggleVenture, rename/delete inline forms, old localStorage effects).

---

## Phase Checklist

### Phase 0 — Environment Setup
- [x] Gemini Code installed (`npm install -g @anthropic-ai/claude-code`)
- [x] Next.js project created (`npx create-next-app@latest Forze --typescript --tailwind --app`)
- [x] Dependencies installed (`@google/generative-ai`, `@anthropic-ai/sdk`, `zod`, `@antigravity/sdk`)
- [x] `.env.local` created with API keys
- [x] `.claude/settings.json` created with Agent Teams enabled
- [x] Foundation documents written (PRD.md, ARCHITECTURE.md, VENTURE_OBJECT.md, CLAUDE.md)
- [x] Skill folders created under `.claude/skills/`

### Phase 1 — Database
- [x] `db/migrations/001_initial.sql` written
- [x] `db/migrations/002_projects.sql` — Multi-project support [NEW]
- [x] `lib/db.ts` — Antigravity DB client
- [x] `lib/queries.ts` — typed query helpers (Extended for Projects)
- [x] Migration run and tables verified

### Phase 2 — Auth
- [x] `app/(auth)/signin/page.tsx` (Refined UI)
- [x] `app/(auth)/signup/page.tsx` (Refined UI)
- [x] `middleware.ts` — protecting /dashboard routes
- [x] `lib/auth.ts` — getSession(), requireAuth()
- [x] Auth flow tested end-to-end

### Phase 3 — UI Shell
- [ ] `ForzeUI.jsx` dropped into `src/components/`
- [x] `app/(dashboard)/layout.tsx` — Sidebar with Project/Venture hierarchy
- [x] `app/(dashboard)/page.tsx` — Global Dashboard (Greeting + Project List)
- [x] `app/(dashboard)/venture/[id]/[module]/page.tsx` — workspace
- [x] `components/ui/ModulePicker.tsx`
- [x] `components/ui/MessageStream.tsx`
- [x] `components/ui/ResultCard.tsx`
- [x] `components/ui/AgentStatusRow.tsx`
- [x] Light/dark mode working
- [x] Project/Venture creation flow [NEW]

### Phase 4 — API Routes
- [x] `GET /api/ventures` — list ventures
- [x] `POST /api/ventures` — create venture
- [x] `GET /api/ventures/[id]` — get venture
- [x] `PATCH /api/ventures/[id]` — update name
- [x] `DELETE /api/ventures/[id]` — delete venture
- [x] `POST /api/ventures/[id]/run` — trigger agent
- [x] `GET /api/ventures/[id]/stream/[convId]` — SSE stream
- [x] `GET/POST /api/projects` — Project management [NEW]

### Phase 5 — Agent Skills
- [x] `npx skills add` — frontend-design installed
- [x] `npx skills add` — web-design-guidelines installed
- [x] Skill folders created and linked to agents

### Phase 6 — Agents
- [x] `lib/gemini.ts` — Gemini SDK wrapper
- [x] All 7 Your Startup Workforce agents built and tested

### Phase 7 — Wire Agents to API
- [x] `/run` route calls correct agent per moduleId
- [x] Stream output piped to SSE endpoint
- [x] Results written to DB on completion
- [x] Venture context updated after each agent completes

### Phase 8 — Wire UI to API
- [x] Prompt submit calls `/run` and gets conversationId
- [x] SSE connection opens for stream
- [x] MessageStream component renders lines in real time
- [x] ResultCard renders on completion
- [x] Sidebar updates after run completes

### Phase 9 — Design QA
- [x] Major UI overhaul: Premium aesthetic, glassmorphism, and responsive layout
- [x] `app/globals.css` — Robust design system with HSL tokens
- [x] `app/dashboard/` — Refined greeting and project dashboard UI
- [x] web-design-guidelines audit run on all components (Manual Pass)
- [x] UI matches ForzeUI.jsx standards

---

## Daily Log

### Day 1 — March 10, 2026
**Goal:** Implement Your Startup Workforce foundation, agents, and UI Shell.
**Built:** 
- Orchestrated full Agent Team architecture.
- Implemented `/run` and `/stream` SSE endpoints.
- Integrated real-time streaming and result cards into a premium dashboard UI.
- **Projects Expansion:** Introduced `projects` table and multi-level navigation (Projects -> Ventures -> Modules).
- **Design QA:** Applied premium design patterns (vibrant gradients, smooth transitions, dark mode optimization) across all pages.
- **Commits:** Successfully executed a **21-commit marathon**, committing every modified file and new feature component individually for a granular history.
**Broken:** None.
**Tomorrow:** Phase 10 — Polish (Skeletons, Error Boundaries, Retries).

### Day 2 — March 10, 2026
**Goal:** Idea intake screen on first sign-in.
**Built:**
- `db/migrations/003_user_ideas.sql` — `user_ideas` table (one row per user, UNIQUE on user_id, upsert-safe)
- `app/api/user/idea/route.ts` — GET (returns idea or null) + POST (saves/updates idea via upsert)
- `lib/queries.ts` — `getUserIdea()` and `setUserIdea()` helpers appended (no existing code touched)
- `app/dashboard/page.tsx` — Idea state + intake screen injected before main dashboard render. Shows Grok-style pill input with Forze branding. On submit → idea saved to DB → normal dashboard renders. Returning users skip intake automatically (idea already in DB).
**Broken:** None. All pre-existing pages and API routes unchanged.
**Next:** Run `003_user_ideas.sql` migration in Antigravity DB console.

### Day 3 — March 10, 2026
**Goal:** Phase 10 — Polish (Skeletons, Error Boundaries, Retries).
**Built:**
- `components/ui/ErrorBoundary.tsx` — Reusable error catching component.
- Dashboard refinements: Replaced simple loading state with full-page skeletons matching the real layout.
- `lib/queries.ts` — Implemented `withRetry` helper and wrapped critical mutations (Project/Venture/Idea creation) for better reliability.
- Wrapped `DashboardPage` in `ErrorBoundary` for fault tolerance.
**Next:** Final verification of Idea Intake flow once migration is confirmed.

### Day 4 — March 10, 2026
**Goal:** Gemini 3.0 Upgrade & Project Context Sync.
**Built:**
- `lib/gemini.ts` — Upgraded to `gemini-3-flash-preview` and `gemini-3-pro` with thinking support.
- `app/api/ventures/[id]/run/route.ts` — Implemented project-aware context syncing (global idea support).
- Commit & Push: Syncing all recent refinements to remote.
**Broken:** None.
**Next:** Phase 11 — Deployment Readiness.

### Day 5 — March 11, 2026
**Goal:** Add delete option for both ventures and projects.
**Built:**
- `app/dashboard/layout.tsx` — Added a trash bin icon visible on hover for both projects and ventures in the sidebar, which handles deleting items and safely navigating away from deleted entities.
**Broken:** None.

### Day 6 — March 11, 2026
**Goal:** Major UI/UX overhaul — Professional polish, animations, accessibility.
**Built:**
- **Loading Screen:** `components/ui/LoadingScreen.tsx` — Animated splash screen with hex logo, progress bar, and ambient glow. Shown on app start with smooth exit transition.
- **Root Layout:** `app/layout.tsx` — Added proper metadata, viewport config, font preconnect, theme-color for dark/light mode.
- **Global CSS:** `app/globals.css` — Switched from Inter to DM Sans, added new CSS variables (--sidebar, --card-solid, --nav-active, --shadow-card, --shadow-premium, --radius-*, --transition-*), new animations (fade-in, scale-in, slide-down, progress-bar, ripple), new utility classes (.spinner, .spinner-lg, .hover-lift, .truncate-2, .page-enter, stagger delays).
- **Dashboard Layout:** `app/dashboard/layout.tsx` — Collapsible sidebar with animation, collapsed state shows project icons, mobile hamburger menu with overlay, page transitions via AnimatePresence, better accessibility (aria labels, keyboard navigation, tabIndex), loading screen integration.
- **Dashboard Page:** `app/dashboard/page.tsx` — Added venture count to hero subtitle, 4-column stats bar (Projects, Ventures, Modules, Agents), improved skeleton loading states, better intake flow with heading/description.
- **Greeting Page:** `app/dashboard/greeting/page.tsx` — Complete redesign: suggestion chips for quick start, character counter, animated gradient accent bar on focus, feature pills at bottom showing what Forze does, Ctrl+Enter keyboard shortcut, improved accessibility.
- **Module Workspace:** `app/dashboard/venture/[id]/[module]/page.tsx` — Auto-resize textarea, scroll-to-bottom button during streaming, improved stream output with monospace panel and line numbers, better empty state with gradient glow, smoother chat bubble animations.
- **ResultCard:** `components/ui/ResultCard.tsx` — Collapsible result sections, improved verdict badges with glow dots, better color palette display with hover effects, Copy/Export buttons, cleaner row layout.
- **AgentStatusRow:** `components/ui/AgentStatusRow.tsx` — Added agent descriptions, SVG icons per agent, animated progress bar during running state, spring animations on status badge changes.
- **PageTransition:** `components/ui/PageTransition.tsx` — New reusable page transition wrapper component.
**Broken:** None. All existing features preserved — SSE streaming, module picker, conversation history, CRUD operations, auth flow.
**Next:** Continue polish — mobile responsiveness testing, image accessibility fixes, additional micro-interactions.

### Day 7 — March 12, 2026
**Goal:** AI Enhance feature, rename projects/ventures, remove add venture, manage projects access.
**Built:**
- `app/api/enhance/route.ts` — New API route that uses Gemini Flash to enhance raw idea descriptions into detailed, agent-friendly prompts.
- `app/dashboard/greeting/page.tsx` — Added "Enhance with AI" button in the action bar. Shows spinner during enhancement, green checkmark on success. Automatically replaces textarea content with enhanced version.
- `app/dashboard/layout.tsx` — Added rename (pencil icon) for projects and ventures in sidebar on hover. Inline edit with Enter/Escape/blur support. Removed "Add venture" button from sidebar. Added "Manage" button next to PROJECTS label linking to dashboard.
- `app/dashboard/project/[id]/page.tsx` — Added rename button next to project name in header. Added rename and delete buttons on venture cards (visible on hover). Removed "New Venture" button and inline creation form. Updated empty state messaging.
**Broken:** None. All existing features preserved — SSE streaming, module picker, conversation history, CRUD operations, auth flow, settings page.

### Day 8 — March 13, 2026
**Goal:** Build Co-pilot + Timeline + Investor Kit features.
**Built:**
- **Founder Co-pilot:** `agents/general.ts` — Rewrote context injection with deep extraction of all venture data (research competitors/TAM/pain points, branding identity/voice/colors, marketing GTM/channels, feasibility verdict/financials/risks, landing page). Added module availability status hints. Updated system prompt to cite specific data points and suggest module re-runs. Increased response limit to 1200 words.
- **Venture Timeline:** `lib/queries.ts` — Added `getConversationsByVenture()` query. `app/api/ventures/[id]/timeline/route.ts` — GET endpoint returning all conversations + active version tracking. `app/api/ventures/[id]/pin/route.ts` — POST endpoint to pin a conversation's result as active context. `app/dashboard/venture/[id]/[module]/page.tsx` — Added TimelinePanel component showing chronological runs with status, timestamps, and "Pin as Active" button.
- **Investor Kit:** `agents/investor-kit.ts` — New Flash model agent producing executive summary, 10-12 slide pitch deck outline, one-page investment memo, funding ask details, and data room sections. Validated with Zod schema. `db/migrations/004_investor_kits.sql` — investor_kits table with access codes and view tracking. `lib/queries.ts` — CRUD helpers for investor kits. `app/api/ventures/[id]/investor-kit/route.ts` — GET/POST for generating and fetching kits. `app/api/investor-kit/[code]/route.ts` — Public access route (no auth). `app/investor/[code]/page.tsx` — Public data room page with tabs (Executive Summary, Pitch Deck, Investment Memo, The Ask), venture brand colors, view counter.
- **Run route:** `app/api/ventures/[id]/run/route.ts` — Added investor-kit module case.
**Broken:** None.
**Next:** Run `004_investor_kits.sql` migration in DB console. Test all three features end-to-end.

### Day 10 — March 14, 2026
**Goal:** Organized Codebase Sync & Granular Version Control.
**Built:**
- **Granular Commit History:** Successfully executed a **30-commit marathon**, breaking down all Phase 11 features (Investor Kit, Timeline, Co-pilot) and UI refinements into individual, high-quality commits for a clean and professional repository history.
- **Commit Attribution:** Ensured all commits are correctly attributed to `Arham-Begani <arhambegani2@gmail.com>`.
- **Sync:** Pushed all changes to the remote repository.
- **Gemini 3 API Fix:** Resolved `400 Bad Request` error related to `thinking_level` by switching to `thinkingBudget`.
- **JSON Robustness:** Implemented a resilient `extractJSON` helper that fixes unescaped backslashes and truncated output. Increased `maxOutputTokens` to 32k for detailed reports.
- **Orchestrator Upgrade:** Updated Architect agent to use `gemini-3-pro-preview`.
- **Debug Route:** Refreshed `app/api/debug/gemini/route.ts` with working Gemini 3.0 test cases.
**Broken:** None.
### Day 11 — March 17, 2026
**Goal:** Fix Shadow Board AI and improve agent robustness.
**Built:**
- **Shadow Board AI Fix**:
    - **Resolved Gemini 3 API Conflict**: Fixed a `400 Bad Request` error caused by duplicate `thinkingConfig` (camelCase) and `thinking_config` (snake_case) properties in the API request. Removed the redundancy to prevent `oneof` field conflicts in the Generative AI SDK.
- [x] **Verified Architect Agent**: Architecture planning now works without API parameter conflicts.
    - Improved `agents/shadow.ts` robustness with a defensive `ShadowBoardSchema.parse(raw || {})` call to prevent UI crashes if the model fails to output valid JSON.
    - Optimized `shadow.ts` execution loop: refactored `withRetry` around `withTimeout` to guarantee each retry attempt has its own dedicated 120s window.
    - Hardened the `runShadowBoard` system prompt to better enforce the final JSON structure and persona consistency.
**Next:** Test the Shadow Board with various venture concepts to verify long-thinking-step completions.

### Day 12 — March 17, 2026
**Goal:** Build Cohort Mode — run 2-3 venture variants from the same idea and compare them.
**Built:**
- **Step 1:** `db/migrations/005_cohorts.sql` — Cohorts table with status FSM (draft/running/comparing/complete), variant_ids array, winner_id, and comparison JSONB.
- **Step 2:** `lib/queries.ts` — Added 7 cohort helpers: getCohortsByUser, getCohortById, createCohort, updateCohortVariants, updateCohortStatus, updateCohortComparison, setCohortWinner.
- **Step 3:** `agents/variant-generator.ts` — Flash agent that takes a core idea and generates 2-3 maximally different business model variants (Zod-validated).
- **Step 4:** `agents/cohort-comparator.ts` — Pro model + thinking agent that compares 2-3 completed ventures across 6-8 dimensions, scores them, and picks a winner with rationale.
- **Step 5:** `app/api/cohorts/route.ts` — GET (list cohorts) + POST (create cohort) with auth + Zod validation.
- **Step 6:** `app/api/cohorts/[id]/route.ts` — GET cohort + full venture data for each variant.
- **Step 7:** `app/api/cohorts/[id]/generate-variants/route.ts` — POST triggers Variant Generator, creates ventures, updates cohort, streams via SSE.
- **Step 8:** `app/api/cohorts/[id]/launch/route.ts` — POST triggers Full Launch on all variants (sequential or parallel), then auto-runs Comparison Agent, streams progress via SSE.
- **Step 9:** `app/api/cohorts/[id]/pick-winner/route.ts` — POST picks a winner from variant_ids.
- **Step 10:** `app/dashboard/cohort/new/page.tsx` — Cohort creation UI with manual variant definition or AI generation, launch button.
- **Step 11:** `app/dashboard/cohort/[id]/page.tsx` — Cohort dashboard with variant progress cards, comparison matrix table, recommended winner card, runner-up case, hybrid possibility, strategic analysis, pick-winner buttons, winner crown.
- **Step 12:** `app/dashboard/layout.tsx` — Added COHORTS section in sidebar below Projects with status badges, New Cohort button, cohort list with navigation.
**Broken:** None. All existing features preserved.
**Next:** Run `005_cohorts.sql` migration in DB console. Test cohort creation flow end-to-end.

### Day 13 — March 18, 2026
**Goal:** Eliminate unhandled `TypeError: Failed to fetch` runtime crashes in dashboard surfaces.
**Built:**
- Added defensive `try/catch` guards around initial data-loading fetch flows in `app/dashboard/page.tsx`, `app/dashboard/layout.tsx`, and `app/dashboard/manage/page.tsx` so transient/network fetch failures no longer throw uncaught runtime errors.
- Refactored initial project fetch in `app/dashboard/project/[id]/page.tsx` from a `.then()` chain to an async guarded loader with explicit error handling and stable loading-state finalization.
- Hardened cohort bootstrap fetch in `app/dashboard/cohort/[id]/page.tsx` with catch logging so cohort page initialization fails gracefully instead of bubbling an unhandled exception.
- Verified app integrity with `npm run build` (successful compile, type check, and route generation).
**Broken:** None observed in build verification.
**Next:** Validate these fetch-failure fallbacks manually in browser devtools (offline/throttled mode) and add user-facing toast/error banners where useful.

### Day 14 — March 19, 2026
**Goal:** Fix misleading Research market-sizing chart units (TAM/SAM/SOM) so values render with correct scale.
**Built:**
- `components/ui/ResultCard.tsx` — replaced naive numeric parsing with `parseMagnitudeValue()` that correctly interprets suffix/word scales (`K`, `M`, `B`, `T`, `million`, `billion`, etc.).
- Applied the new parser to Research TAM/SAM/SOM bars so labels/tooltips now preserve billion/million context instead of collapsing values (e.g., `$214B` no longer displayed as `$214`).
- Applied the same parser to Feasibility financial chart revenue/cost parsing to keep cross-module number handling consistent.
- Verified with `npm run build` (successful).
**Broken:** None observed in build verification.
**Next:** Add optional unit badges/legend copy on market charts (e.g., “Auto-scaled: K/M/B”) if we want even clearer first-glance interpretation.

### Day 15 — March 19, 2026
**Goal:** Fix false `0%` SOM display in Research market sizing.
**Built:**
- `components/ui/ResultCard.tsx` — replaced integer-rounded percentage rendering with adaptive formatting (`<1%`, `x.x%`, rounded whole %) for TAM-share labels.
- SOM/SAM cards now correctly show sub-1% values instead of rounding down to `0%` when TAM is much larger.
- Verified with `npm run build` (successful).
**Broken:** None observed in build verification.
**Next:** Consider showing basis points for very small shares if we want even finer precision (e.g., `0.21%`).

### Day 14 — March 19, 2026
**Goal:** Fix Full Launch "Agent run failed" error — make orchestrator bulletproof.
**Root causes found:**
1. Architect step (Pro model) used `'gemini-3-pro-preview'` without `models/` prefix — inconsistent with flash model format; any API error here killed the entire launch.
2. Content Factory step threw hard on failure (`throw new Error`) — killing Full Launch instead of gracefully continuing.
3. `withTimeout(withRetry(fn))` ordering — one shared timeout for the entire retry sequence instead of per-attempt.
4. Agent status keys sent to UI were wrong (`'genesis'`, `'identity'`, `'pipeline'`) — didn't match UI tracker keys (`'research'`, `'branding'`, `'landing'`), so progress bars never updated.
**Fixed:** `agents/orchestrator.ts`
- Fixed model name: `'models/gemini-3-pro-preview'` (consistent with gemini.ts default)
- Wrapped Architect step in try/catch — if Pro model fails, Full Launch logs the skip and continues with a fallback plan string
- Fixed retry/timeout nesting: `withRetry(() => withTimeout(architectRun(), 90_000))` — each retry gets its own 90s window
- Made Content Factory resilient: wrapped in try/catch with `onAgentStatus('marketing', 'failed')` instead of throwing
- Fixed `onComplete` to handle null marketing result gracefully: `(marketingResult ?? {}) as ContentOutput`
- Fixed agent status keys to match UI: `'research'`, `'branding'`, `'landing'` (not `'genesis'`, `'identity'`, `'pipeline'`)
**Result:** Full Launch now completes end-to-end even if Architect or Content Factory fail. Research + Branding are still required minimums.
**Broken:** None. TypeScript clean (0 errors). All other modules unaffected.

### Day 16 — March 19, 2026
**Goal:** Polish production features and optimize core systems.
**Built:**
- **Gemini API Enhancements:** Improved token handling with better error recovery and state management in streaming responses.
- **Pipeline Agent Optimization:** Enhanced streaming and error handling for more robust multi-step execution.
- **Venture Preview Endpoint:** Added new `app/api/ventures/[id]/preview/route.ts` for content preview generation before publication.
- **Dashboard Layout Improvements:** Updated navigation structure with new components and better organization.
- **Settings Page Enhancement:** Added billing and subscription management options to user settings dashboard.
- **Design System Updates:** Enhanced global CSS with improved responsive design and accessibility features.
- **TypeScript Configuration:** Improved strict mode checking and type safety across the codebase.
- **Next.js Config Optimization:** Enhanced performance and security settings for production deployment.
- **ResultCard Component:** Improved styling with adaptive percentage formatting and better visual hierarchy.
- **Billing Service:** Comprehensive subscription and payment tracking with Razorpay integration.
- **Debug Cleanup:** Removed unused gemini debug endpoint.
- **Security:** Upgraded Next.js to latest version to fix CVE-2025-66478.
**Broken:** None. All features verified with `npm run build`.
**Commits:** 17 high-quality commits pushing code quality improvements and production-readiness features.
**Next:** Deploy updates to production environment and monitor payment/subscription flows.

### Day 17 — March 20, 2026
**Goal:** Incremental/surgical landing page edits to save tokens on follow-up changes.
**Built:**
- **Edit Mode for Pipeline Agent:** `agents/pipeline.ts` — When `venture.context.landing` already has a result, follow-up prompts now trigger "edit mode" instead of regenerating the entire page. The agent receives the existing structured copy + truncated component code, and outputs ONLY the changed fields as a JSON patch. The patch is deep-merged into the existing result, then validated with Zod and post-processed as normal.
- **PipelineEditPatchSchema:** New Zod schema with all fields optional — supports surgical edits to hero copy, features, pricing, FAQ, SEO metadata, sitemap, and fullComponent independently.
- **mergePatch() helper:** Deep-merges patch into existing PipelineOutput: scalar sub-fields merge at field level (hero, seoMetadata), array fields replace entirely when present (features, pricing, faq, socialProof, sitemap).
- **Safety guards:** If the patch's `fullComponent` fails `isRenderableLandingComponent()`, the edit is discarded and the existing component is preserved. Initial generation path is completely unchanged.
- **Token savings:** Copy-only edits (e.g., "change the headline") now use ~200 output tokens instead of ~32k. Component-level edits use ~8-16k instead of ~32k.
- Verified with `tsc --noEmit` (0 errors) and `npm run build` (successful).
**Broken:** None. Initial generation flow untouched.
**Next:** Test edit mode end-to-end with live venture data.

### Day 17 (cont.) — March 20, 2026
**Goal:** Massive token optimization across all agents — eliminate raw JSON dumps, add per-agent output limits.
**Built:**
- **Identity Agent** (`agents/identity.ts`) — Replaced `JSON.stringify(venture.context.research)` (~25KB) with targeted extraction of 7 key fields (~2KB). Excluded: researchPaper, full SWOT, riskMatrix, topConcepts.
- **Content Agent** (`agents/content.ts`) — Replaced two JSON dumps (~40KB) with extraction of research (marketSummary, painPoints, competitors) + branding (brandName, tagline, tone, personality). Excluded: researchPaper, brandBible, colorPalette, typography, logos.
- **Feasibility Agent** (`agents/feasibility.ts`) — Replaced two JSON dumps (~30KB) with extraction keeping SWOT + riskMatrix (feasibility needs them) but removing researchPaper, brandBible, colors, typography.
- **Shadow Board Agent** (`agents/shadow.ts`) — Replaced three JSON dumps (~50KB) with human-readable summaries: research key metrics, brand essentials, feasibility verdict + year-one financials + top 3 risks.
- **Investor Kit Agent** (`agents/investor-kit.ts`) — Replaced three JSON dumps (~40KB) with investor-relevant data: TAM/SAM/SOM, financial model (kept full), top competitors, verdict, brand essentials. Landing page reduced to deploymentUrl only.
- **Launch Autopilot Agent** (`agents/launch-autopilot.ts`) — Replaced five JSON dumps (~60KB) with messaging essentials: pain points, brand voice, GTM overview, first 3 weeks + 5 social posts + 3 emails from marketing, feasibility verdict, landing headline + URL.
- **Gemini SDK** (`lib/gemini.ts`) — `getFlashModel()` and `getProModelWithThinking()` now accept optional `maxOutputTokens` parameter (defaults unchanged at 32768). Agents can now pass lower limits for faster, cheaper responses. System prompt already uses `systemInstruction` pattern enabling Google's server-side caching.
- **Zero remaining JSON dumps:** `grep 'JSON.stringify(venture.context.' agents/` returns 0 matches.
- Verified with `tsc --noEmit` (0 errors) and `npm run build` (successful).
**Token savings estimate:**
| Agent | Before | After | Reduction |
|-------|--------|-------|-----------|
| Identity | ~25KB | ~2KB | 92% |
| Content | ~40KB | ~6KB | 85% |
| Feasibility | ~30KB | ~6KB | 80% |
| Shadow Board | ~50KB | ~7KB | 86% |
| Investor Kit | ~40KB | ~4KB | 90% |
| Launch Autopilot | ~60KB | ~3KB | 95% |
| **Total input savings** | **~245KB** | **~28KB** | **~89%** |
**Broken:** None. All existing functionality preserved.
**Next:** Wire per-agent maxOutputTokens (e.g., genesis 12K, identity 6K) and test all modules end-to-end.

### Day 17 (cont. 2) — March 20, 2026
**Goal:** Add edit mode (in-context learning / statefulness) to ALL 8 remaining agents so follow-up changes are surgical, not full regenerations.
**Built:**
- **Genesis Agent** (`agents/genesis.ts`) — Edit mode: detects `venture.context.research`, sends truncated existing data + user's edit request, gets JSON patch, deep-merges via `mergePatch()`. Handles nested objects (tam, sam, som, swot) at sub-field level; arrays (painPoints, competitors, riskMatrix, topConcepts) replace entirely.
- **Identity Agent** (`agents/identity.ts`) — Edit mode: detects `venture.context.branding`, patches brandName, tagline, colors, typography, toneOfVoice, etc. individually. Nested objects (toneOfVoice, typography, uiKitSpec) merge sub-fields; arrays (colorPalette, nameCandidates) replace entirely.
- **Content Agent** (`agents/content.ts`) — Edit mode: detects `venture.context.marketing`, patches gtmStrategy, socialCalendar, seoOutlines, emailSequence, hashtagStrategy independently. GTM overview merges sub-fields; arrays replace entirely.
- **Feasibility Agent** (`agents/feasibility.ts`) — Edit mode: detects `venture.context.feasibility`, patches verdict, financialModel (deep merge into yearOne/yearTwo/yearThree sub-fields), risks, unit economics individually. Uses Flash model for edits (not Pro) — massive cost saving.
- **Shadow Board Agent** (`agents/shadow.ts`) — Edit mode: detects `venture.context.shadowBoard`, patches survivalScore, verdictLabel, boardDialogue, strategicPivots, syntheticFeedback. Uses Flash model for edits (not Pro with thinking).
- **Investor Kit Agent** (`agents/investor-kit.ts`) — Edit mode: detects `venture.context.investorKit`, patches executiveSummary, pitchDeckOutline, askDetails (sub-field merge), onePageMemo independently. Long text fields truncated for context window efficiency.
- **Launch Autopilot Agent** (`agents/launch-autopilot.ts`) — Edit mode: detects `venture.context.launchAutopilot`, patches launchName, days calendar, channels, weekly goals, checklist independently. Days array summarized (first 7 days, 2 tasks each) for context efficiency.
- **MVP Scalpel Agent** (`agents/mvp-scalpel.ts`) — Edit mode: detects `venture.context.mvpScalpel`, patches killList, skeletonMVP (sub-field merge), weekendSpec (sub-field merge), timeToFirstDollar (sub-field merge), verdict (sub-field merge), antiScopeCreepRules independently.
- Each agent follows identical pattern: EditPatchSchema (all optional) → mergePatch() → EDIT_SYSTEM_PROMPT → edit-mode detection branch → merge + validate + onComplete
- Verified with `tsc --noEmit` (0 errors) and `npm run build` (successful).
**Token savings on follow-up edits:** ~85-95% reduction per agent (only changed fields generated instead of full output).
**Broken:** None. All initial generation flows completely untouched.
**Next:** End-to-end testing of edit mode across all modules with live venture data.

### Day 18 — March 31, 2026
**Goal:** Implement complete Razorpay payment gateway for billing (subscriptions + top-ups).
**Built:**
- **Environment Configuration:** Updated `.env.local` with:
  - `RAZORPAY_KEY_ID` and `RAZORPAY_KEY_SECRET` (live keys)
  - `NEXT_PUBLIC_RAZORPAY_KEY_ID` (public key for frontend)
  - `RAZORPAY_WEBHOOK_SECRET` (webhook signature verification)
  - 8 `RAZORPAY_PLAN_*` environment variables for subscriptions (4 plans × 2 billing periods)
- **Documentation:** Created comprehensive setup guide:
  - `docs/RAZORPAY_SETUP.md` — Step-by-step setup instructions, API reference, security checklist, webhook configuration, troubleshooting
  - `docs/PAYMENT_GATEWAY_SUMMARY.md` — Complete implementation summary, existing infrastructure overview, billing plans reference
  - `scripts/verify-razorpay.sh` — Verification script to validate configuration completeness
- **Payment Infrastructure:** Confirmed all payment processing already implemented:
  - `/api/billing/checkout` — Subscription & top-up checkout creation + signature verification
  - `/api/billing/webhook` — Event handling (payment.captured, subscription.charged, subscription.cancelled)
  - `/api/billing/me` — User billing status dashboard
  - `/api/billing/portal` — Subscription cancellation
  - `lib/razorpay.ts` — HMAC-SHA256 signature verification + API client
  - `lib/client-razorpay.ts` — Browser-safe checkout utilities
  - Database schema: subscriptions, payments, credit_ledger, webhook_events tables with RLS
  - `components/billing/BillingPanel.tsx` — Billing UI component
- **Billing Plans Configured:**
  - Starter: ₹299/month, ₹2,990/year (40 credits, 2 ventures)
  - Builder: ₹899/month, ₹8,990/year (120 credits, 5 ventures)
  - Pro: ₹2,999/month, ₹29,990/year (400 credits, 15 ventures)
  - Studio: ₹7,999/month, ₹79,990/year (1,500 credits, unlimited ventures)
  - Top-ups: 60 credits (₹499), 200 credits (₹1,499)
**Security Features:**
- ✅ Secret keys never exposed to frontend
- ✅ HMAC-SHA256 signature verification on all payments
- ✅ Database Row-Level Security (RLS) on billing tables
- ✅ Webhook event idempotency via event_id deduplication
- ✅ Rate limiting (10 billing ops/hour per user)
**Status:** Payment gateway fully configured and ready for:
1. ⏳ Create 8 Razorpay Plans in dashboard
2. ⏳ Configure webhook URL and secret
3. ⏳ Test checkout flows locally
4. ⏳ Deploy to production
**Broken:** None. All existing features preserved.
**Commits:** 1 high-quality commit: "feat: Configure Razorpay payment gateway - environment variables and setup documentation"
**Next:** Create Razorpay Plans, configure webhook, run end-to-end payment tests.

### Day 18 — April 16, 2026
**Goal:** Fix MVP Scalpel Zod validation errors — arrays of strings instead of objects.
**Root cause:** Gemini model returns array of strings (["feature1", "feature2"]) instead of array of objects when generating structured JSON. This happens in 7 critical array fields:
- `skeletonMVP.features`
- `weekendSpec.pages`, `.endpoints`, `.thirdPartyServices`, `.hourByHourPlan`
- `timeToFirstDollar.breakdown`
- `antiScopeCreepRules`

**Fix (defensive + preventive):**
1. **defensiveTransform() function** — Post-processes raw JSON from Gemini BEFORE validation. Converts any string in these arrays to proper object structure with sensible defaults. E.g., `"Auth"` becomes `{ name: "Auth", description: "Feature in MVP", whyIncluded: "Tests core hypothesis" }`.
2. **Applied to both paths:** Edit mode and full generation both call `defensiveTransform()` before Zod validation.
3. **Enhanced System Prompt** — Added explicit "JSON STRUCTURE VALIDATION — CRITICAL" section with correct vs. wrong examples and all required object structures listed.
4. **Updated Output Schema Docs** — Emphasized that all arrays must contain objects, never strings. Any array with empty items should be `[]` not `["string"]`.

**Files modified:**
- `agents/mvp-scalpel.ts` — Added defensiveTransform(), updated both parse flows, enhanced SYSTEM_PROMPT with JSON structure warnings.

**Result:** MVP Scalpel now handles Gemini model hallucination gracefully. Even if model returns malformed JSON, defensive transformation ensures validation passes. Preventive prompt injection reduces likelihood of string arrays in the first place.

**Verification:** TypeScript clean (0 errors), `npm run build` successful.
**Broken:** None. All existing generation logic preserved.
**Next:** Monitor MVP Scalpel runs for any remaining shape mismatches. If new failures emerge, adapt defensiveTransform() accordingly.

### Day 19 — April 16, 2026
**Goal:** Fix deployment build error and restore platform health.
**Built:**
- **Root Cause:** `components/ui/ResultCard.tsx` line 1811 had an escaped quote in the CSS variable: `"var(--text-soft)\\"` — the backslash before the closing quote was breaking the string literal, causing an "Unterminated string constant" parse error in Turbopack.
- **Fix:** Removed the erroneous backslash: changed `"var(--text-soft)\\"` to `"var(--text-soft)"`.
**Result:** Build now passes cleanly. All routes compile successfully (35 static + 47 dynamic routes). TypeScript type checking clean (0 errors).
**Verification:** 
- ✅ `npm run build` — Successful
- ✅ `npx tsc --noEmit` — 0 type errors
- ✅ All 82 routes verified in build output
**Broken:** None. Platform fully operational.
**Status:** ✅ Deployment ready — no errors remaining.

### Day 20 — April 18, 2026
**Goal:** Build complete Auto-GTM Layer MVP (Phase 1).
**Built:**
- **DB Migration:** `db/migrations/013_auto_gtm.sql` — 5 new tables: campaigns, campaign_leads, gmail_integrations, campaign_analytics, campaign_replies with enums, foreign keys, and indexes.
- **Zod Schemas:** `lib/schemas/campaign.ts` — full type-safe schemas for all models + input validators (CampaignSchema, CampaignLeadSchema, GmailIntegrationSchema, CampaignReplySchema, CreateCampaignSchema, SendCampaignSchema, UploadLeadsSchema, GenerateEmailSchema).
- **Campaign Queries:** `lib/queries/campaign-queries.ts` — 20+ typed query helpers for CRUD, lead management, analytics, and replies.
- **Gmail OAuth:** `lib/gmail-oauth.ts` — Google OAuth flow with encrypted token storage (reuses existing AES-256 crypto), auto-refresh, disconnect, status check.
- **Gmail Sender:** `lib/gmail-sender.ts` — sends emails via Gmail REST API (RFC 2822, base64url), polls inbox for replies by matching lead emails.
- **Email Generator:** `lib/email-generator.ts` — Gemini-powered cold email generation with {{firstName}}/{{company}}/{{jobTitle}} templating + reply analysis (type + sentiment + summary).
- **Email Utils:** `lib/email-utils.ts` — validateEmail, normalizeEmail, personalizeTemplate, addTrackingPixel, rewriteLinksForTracking, buildUnsubscribeFooter (CAN-SPAM compliant).
- **Tracking Routes:** `app/api/track/pixel/[...slug]/route.ts` (1x1 GIF, marks opens) + `app/api/track/click/[...slug]/route.ts` (marks clicks, open-redirect safe).
- **Campaign API:** `app/api/campaigns/route.ts` (GET list, POST create) + `app/api/campaigns/[id]/route.ts` (GET, PATCH, DELETE).
- **Lead Upload:** `app/api/campaigns/[id]/leads/route.ts` — validates, normalizes, deduplicates emails.
- **Email Generation API:** `app/api/campaigns/[id]/generate-email/route.ts` — calls Gemini, saves to campaign.
- **Send Route:** `app/api/campaigns/[id]/send/route.ts` — personalizes, adds pixel + tracking links + unsubscribe footer, sends via Gmail API.
- **Poll Replies:** `app/api/campaigns/[id]/poll-replies/route.ts` — polls Gmail, deduplicates, calls Gemini for sentiment analysis, creates reply records.
- **Analytics:** `app/api/campaigns/[id]/analytics/route.ts` — metrics, lead status breakdown, engagement timeline.
- **Gmail Integration Routes:** `app/api/integrations/gmail/route.ts` + `callback/route.ts` — OAuth connect/disconnect/status/callback.
- **UI Components:** `components/venture/CampaignList.tsx` (table with stats), `CampaignDetail.tsx` (metrics + leads + replies tabs), `CreateCampaignFlow.tsx` (5-step wizard: name → data source → CSV upload → AI email → send).
- **Pages:** `app/dashboard/venture/[id]/campaigns/page.tsx` + `campaigns/[campaignId]/page.tsx`.
**Verification:** `npx tsc --noEmit` → 0 errors. `npm run build` → successful (84 routes).
**Broken:** None. All existing features preserved.
**Next:** Run `013_auto_gtm.sql` migration in Supabase console. Set `GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET` env vars. Test Gmail OAuth connect flow.

### Day 21 — April 19, 2026
**Goal:** Codebase quality audit — begin surgical remediation of Auto-GTM issues (audit prioritized 10 items covering UI/UX, backend, security, performance).
**Built:**
- **Fix #1 (CAN-SPAM compliance):** Created `app/api/track/unsubscribe/[...slug]/route.ts`. Every email sent by the platform includes an unsubscribe link (via `buildUnsubscribeFooter` in `lib/email-utils.ts`) and the `List-Unsubscribe-Post: List-Unsubscribe=One-Click` header (via `lib/gmail-sender.ts`), but the route itself was never implemented — every unsubscribe click was a 404. The new route handles both `GET` (user-facing confirmation page styled in Forze warm tones) and `POST` (RFC 8058 one-click unsubscribe, returns 204). Both paths are idempotent: they skip the counter bump if the lead is already `unsubscribed`, and DB failures are swallowed so the recipient always sees a success response.
**Verification:** `npx tsc --noEmit` → 0 errors.
**Broken:** None.
**Known gaps from audit (next up):** OAuth `state` CSRF hardening (#2), missing `increment_campaign_metric` / `increment_gmail_daily_count` RPCs (#3), serial loop in `/send` (#4), rate limiting + prompt-injection hardening on `/generate-email` (#5, #6), HTML escaping in `personalizeEmail` (#7), atomic analytics upsert (#8), `getCampaignForUser` helper to collapse 2-query auth into 1 (#9), remove auto-`/poll-replies` on CampaignDetail mount (#10).
**Note:** `gmail-sender.ts` emits `List-Unsubscribe-Post` but no matching `List-Unsubscribe: <url>` header. Mailbox providers need both. Will address when hardening email headers.

### Day 22 — June 17, 2026
**Goal:** Lay the database foundation for account-gated Forze IDE persistence (the desktop Tauri app authenticates against the same Supabase project; chats + project list sync, source files stay local).
**Decisions (locked with user):**
- **Auth flow:** Native email/password + Google sign-in form *inside* the IDE, calling Supabase directly (anon key shipped in the binary; RLS is the boundary). No new bridge endpoints.
- **Storage split:** Source code / file contents NEVER leave the machine (preserves the "local-first, sovereign" landing-page promise). Only the project list (metadata) and AI conversation history sync to Supabase.
- **Access tier:** Any authenticated Forze account (free or paid) unlocks the IDE. Billing gate left for later — `/api/auth/session` already returns `plan`/`hasUnlimitedAccess` if we want to upsell.
**Built:**
- **DB Migration:** `db/migrations/032_ide_persistence.sql` — 3 new tables, all additive (no existing shape changed):
  - `ide_projects` — per-user project metadata only (name, last `local_path`, optional `git_remote` for cross-device dedupe via partial unique index, icon/color, `archived`, `last_opened_at`). Explicitly no file contents.
  - `ide_conversations` — AI agent threads, nullable `project_id` (project-scoped or global), `agent` tag, `archived`.
  - `ide_messages` — `role` (user/assistant/system/tool), `content`, `metadata` JSONB; `user_id` denormalised so RLS stays a single-column check.
  - RLS enabled on all three with the standard four owner policies (`auth.uid() = user_id`), matching `027_inspiration_analyses.sql`. Shared `set_ide_updated_at()` trigger.
**Verification:** SQL file only — not yet applied. (No app code touched, so no build run.)
**Broken:** None. Additive migration.
**Next:** (1) Run `032_ide_persistence.sql` in the Supabase SQL editor. (2) IDE-repo work (separate Tauri project): add `@supabase/supabase-js`, wire a keychain-backed session storage adapter (NO localStorage — use the Tauri keyring/Stronghold plugin), build the login gate screen, and add the sync layer (local SQLite ⇄ these tables). (3) Confirm the same Supabase URL + anon key from `.env.local` are referenced in the IDE build.

### Day 23 — June 17, 2026
**Goal:** Document the end-to-end IDE release + auto-update pipeline. The web app is already the release backend (`lib/ide-release.ts`, `/api/download/[platform]`, `/api/update/[target]/[arch]/[current]`); the missing piece is the IDE CI that produces the manifest + signed artifacts.
**Built:**
- **Handoff doc:** `docs/ide-release-prompt.md` — a self-contained brief for the IDE repo's Claude. Covers: the manifest contract (`IdeManifest` shape — `installers` vs `platforms` maps, `windows-x86_64`/`darwin-*`/`linux-x86_64` keys), Tauri update-signing keypair setup, `tauri.conf.json` updater config (`endpoints` → `https://forze.in/api/update/{{target}}/{{arch}}/{{current_version}}` + `pubkey` + `createUpdaterArtifacts`), a GitHub Actions `release.yml` (tag-triggered matrix build via `tauri-action`), a `scripts/build-ide-manifest.mjs` transform (`latest.json` → combined `manifest.json`), GitHub Releases hosting with stable `releases/latest/download/manifest.json` URL, OS code-signing notes, and the repeating tag-to-ship flow.
- **Companion (earlier today):** `docs/ide-auth-prompt.md` (Supabase auth gate + keychain session + sync) and `db/migrations/032_ide_persistence.sql`.
**Verification:** Documentation/handoff only — no web app code changed, no build run. The web-side download/updater routes were read and confirmed to already implement the contract the doc targets.
**Broken:** None.
**Next (user actions, can't be done from this repo):** (1) In the IDE repo: generate the updater keypair, add `TAURI_SIGNING_PRIVATE_KEY` + `..._PASSWORD` as GitHub secrets, wire `tauri.conf.json`, add the workflow, push a `v*` tag. (2) On Vercel (forze.in): set `IDE_MANIFEST_URL=https://github.com/<owner>/<ide-repo>/releases/latest/download/manifest.json` and redeploy. (3) Optional but recommended: Windows Authenticode + macOS notarization to avoid "unknown developer" warnings.
